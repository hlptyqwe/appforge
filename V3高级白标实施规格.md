# AppForge V3 高级白标实施规格

## 1. 目标

V3 在 V2 基础白标之上提供真正的多品牌产品能力：

- 动态 packageName/applicationId。
- 每个白标产品选择独立签名配置。
- 可复用、可版本化的白标模板。
- Firebase、OAuth、深链和运行时配置等受控扩展文件。

## 2. 前置条件

- V2 全部验收通过。
- 源 APK 已通过资源重建兼容性预检。
- Builder 工具链版本固定并可复现。
- 品牌配置已经采用不可变构建快照。

## 3. 范围

### 3.1 包含

- 白标模板创建、修订、发布、停用和复制。
- 模板参数 Schema 和严格白名单。
- packageName 替换及引用一致性校验。
- 独立签名策略与证书指纹检查。
- `google-services.json`、OAuth redirect scheme、App Links host 等受控配置。
- 模板预检、模板构建和产物安装验证。

### 3.2 不包含

- 执行客户任意 shell/Gradle 脚本。
- 托管客户 Android 源码。
- iOS 白标。
- 在线模板市场和第三方模板交易。

## 4. 核心对象

### 4.1 白标模板 `t_white_label_template`

字段：

```text
id, tenant_id, app_id, template_code, template_name
source_version_id, parameter_schema, compatibility_rules
status, published_revision, create_by, create_time, update_time
```

`template_code` 在租户内唯一。只有发布状态模板可以创建正式构建。

### 4.2 模板修订 `t_white_label_template_revision`

字段：

```text
id, tenant_id, template_id, revision
package_name_rule, manifest_patch, resource_patch
extension_files, expected_artifacts, checksum
status, create_by, create_time
```

修订发布后不可原地修改，只能创建新修订。

### 4.3 白标产品 `t_white_label_product`

字段：

```text
id, tenant_id, app_id, product_code, product_name
template_id, template_revision, branding_profile_id
package_name, signing_config_id, parameter_values
status, create_by, create_time, update_time
```

约束：

- `(tenant_id, package_name)` 唯一。
- packageName 符合 Android applicationId 规则和租户允许前缀。
- 模板、品牌、签名配置和源版本必须属于同一租户。

### 4.4 构建快照

构建任务增加 `white_label_product_id`、`template_revision` 和 `template_snapshot`。任务执行期间不读取可变模板。

## 5. 模板补丁协议

模板不保存任意脚本，只允许声明式操作：

- `manifest.setPackage`
- `manifest.setAttribute`
- `manifest.replaceProviderAuthority`
- `manifest.replaceIntentScheme`
- `manifest.replaceAppLinkHost`
- `resource.replaceString`
- `resource.replaceFile`
- `asset.writeJson`
- `extension.writeValidatedFile`

每种操作具有固定参数 Schema、目标路径白名单和输出限制。禁止路径穿越、外部 URL 下载和命令执行。

## 6. packageName 替换

Builder 必须处理并验证：

- Manifest package 和组件相对类名。
- Provider authorities。
- Permission 名称。
- FileProvider、ContentProvider 和 WorkManager 初始化项。
- OAuth redirect scheme 和 App Links。
- 资源、XML、smali 中属于配置范围的原包名引用。

默认不重写业务代码中的任意字符串。预检发现未声明的旧包名关键引用时必须拒绝构建，并给出位置摘要。

## 7. 签名策略

- 白标产品显式绑定 `signing_config_id`。
- 同 packageName 的升级包必须使用相同证书指纹。
- 系统保存 packageName 与证书 SHA-256 的历史绑定。
- 更换签名需要单独审批状态和审计记录，不允许静默替换。
- 外部 KMS/Vault 仍按接口抽象；完整远程签名可在 V7 企业交付中启用。

## 8. API/RPC

提供模板、模板修订、白标产品的完整 CRUD、发布和预检 API：

```text
/core/white-label/templates
/core/white-label/templates/:id/revisions
/core/white-label/templates/:id/publish
/core/white-label/products
/core/white-label/products/:id/preflight
```

构建任务支持 `whiteLabelProductId`，并返回模板和产品快照摘要。

## 9. 前端

新增：

- 白标模板列表和修订编辑器。
- 参数 Schema 表单设计器。
- 白标产品配置向导。
- packageName 与证书指纹风险提示。
- 模板预检报告、构建历史和产物下载。

模板编辑器只生成声明式 JSON，不提供脚本输入框。

## 10. 安全

- extension file 按类型限制大小、MIME 和 JSON Schema。
- Firebase/OAuth 文件中的 secret 字段按敏感数据策略保存和审计脱敏。
- 参数 Schema 可用 `sensitive: true` 标记敏感字符串；API 使用 AES-256-GCM 加密后入库，查询返回 `***`，Builder 仅在受信执行阶段解密。模板文件和内联补丁禁止保存明文密码、私钥、OAuth client secret 与访问令牌。
- 模板修订校验 checksum，构建日志记录 revision/checksum，不记录 secret 内容。
- packageName、authority、scheme 和路径全部使用严格语法校验。
- 模板发布和签名变更需要独立权限。

## 11. 实施顺序

1. 模板、修订、产品和证书绑定模型。
2. 声明式补丁 Schema、校验器和预检。
3. Builder packageName/Manifest/资源补丁执行器。
4. 模板与产品前端。
5. 真实升级安装、深链、Provider 和 OAuth 场景验收。

## 12. 验收标准

1. 已发布模板修订不可修改，历史构建可追溯 checksum。
2. 非法路径、任意脚本和 Schema 外参数全部被拒绝。
3. 构建产物 packageName 与白标产品一致，源包名关键配置无残留。
4. 同 packageName 使用错误签名证书时不能构建。
5. AppName、Logo、启动图、API Host 继续满足 V2 验收。
6. Provider authority、OAuth scheme、App Links 和扩展文件符合模板配置。
7. 两个不同白标产品可从同一模板并行构建且数据隔离。
8. 新版本产物可以覆盖安装同 packageName、同证书的旧版本。
9. APK 签名、安装和首次启动通过。
10. 全量测试、前端构建和 Docker 重建通过。

只有全部验收有运行证据，V3 才能标记完成。

当前逐项证据和待执行设备门禁见 [V3 高级白标验收报告](./V3高级白标验收报告.md)。
