# AppForge V5 开放平台实施规格

## 1. 目标

V5 将管理页面能力开放为稳定、可审计、可自动化的开发者平台：

- Open API 与 API Key。
- Webhook 事件推送。
- CLI 和 CI/CD 调用。
- GitHub/GitLab 集成。

## 2. 前置条件

- V1–V4 验收通过。
- 构建任务具备取消、重试、并发控制和稳定事件状态。
- 所有资源拥有稳定 ID、状态机和租户隔离。

## 3. Open API

### 3.1 API 版本

开放接口统一使用：

```text
/open/v1/...
```

不直接暴露内部管理 API。破坏性变更必须发布新版本并提供迁移窗口。

### 3.2 API 凭证

新增 `t_open_api_credential`：

```text
id, tenant_id, name, key_id, secret_hash
scopes, app_ids, ip_allowlist
status, expires_at, last_used_at
create_by, create_time, update_time
```

Secret 只在创建时返回一次，数据库只保存不可逆验证值或受控密钥材料。支持轮换、吊销和双 Key 过渡。

### 3.3 权限范围

最少包含：

- `apps:read/write`
- `versions:read/write`
- `channels:read/write`
- `branding:read/write`
- `builds:read/write`
- `artifacts:read`
- `stats:read`

凭证可限制到指定应用，不能通过请求参数扩大范围。

### 3.4 接口能力

- 应用、版本、渠道、品牌和白标产品查询与管理。
- 获取上传票据并完成上传。
- 创建、查询、取消和重试构建。
- 获取短时产物下载地址。
- 查询归因统计。

接口使用统一错误码、幂等键、游标分页、请求 ID 和速率限制响应头。

## 4. Webhook

### 4.1 数据模型

新增：

- `t_webhook_endpoint`：URL、事件、secret 密文、状态。
- `t_webhook_delivery`：事件 ID、attempt、状态、响应码、下次重试时间和错误摘要。
- `t_outbox_event`：业务事务内写入的可靠事件。

### 4.2 事件

至少包括：

```text
build.queued
build.started
build.succeeded
build.failed
build.canceled
artifact.expiring
quota.warning
quota.exceeded
```

### 4.3 投递协议

- JSON body 包含 eventId、eventType、occurredAt、tenantId、data 和 schemaVersion。
- 使用 timestamp + body 的 HMAC-SHA256 签名。
- 指数退避重试，达到上限进入死信状态。
- 相同 eventId 可重复投递，接收方按 eventId 幂等。
- 禁止访问内网、环回、云元数据地址，防止 SSRF。

## 5. CLI

提供 `appforgectl`：

```text
appforgectl auth configure
appforgectl app list
appforgectl version upload
appforgectl build create
appforgectl build wait
appforgectl artifact download
```

CLI 支持 JSON 输出、非交互模式、超时、重试和明确退出码。凭证写入系统安全存储或权限为 `0600` 的配置文件。

## 6. CI/CD

提供官方示例：

- GitHub Action。
- GitLab CI Template。
- 通用 shell/CLI 示例。

标准流程：上传 APK → 创建版本 → 创建构建 → 等待结果 → 下载产物/读取 URL。CI 日志不得打印 Secret 或短时下载签名参数。

## 7. GitHub/GitLab 集成

V5 只做受控集成，不执行任意仓库代码：

- OAuth/App 安装与仓库授权。
- 从 Release Artifact 或 CI Artifact 拉取 APK。
- Commit SHA、pipeline/job 和 artifact SHA 记录到版本来源。
- Webhook 触发预定义构建策略。

访问令牌加密保存，权限最小化，支持吊销和安装状态同步。

## 8. API/RPC 与后台任务

- Open API 使用独立认证中间件和限流桶。
- Webhook outbox 与业务写入同事务，投递由独立 Worker 完成。
- 集成令牌、Webhook secret 通过 secretbox/KMS 加密。
- 所有开放调用写 API 审计，记录 keyId、scope、资源和 requestId，不记录 secret。

## 9. 前端

新增“开发者中心”：

- API Key 创建、轮换、吊销和最近使用。
- Webhook 配置、签名 secret 重置、投递日志和重放。
- GitHub/GitLab 连接与仓库授权。
- OpenAPI 文档、CLI 下载和 CI 示例。

## 10. 实施顺序

1. API Key 模型、认证、scope 和 OpenAPI v1。
2. outbox、Webhook Worker、签名和重试。
3. CLI 与端到端自动化流程。
4. GitHub/GitLab OAuth、Artifact 和触发器。
5. 文档、SDK 示例、安全和故障验收。

## 11. 验收标准

1. API Secret 只显示一次，数据库和日志中不存在明文。
2. scope、应用范围、租户、IP 白名单和过期时间均实际生效。
3. 相同幂等键不会重复创建版本或构建任务。
4. Open API 可以完成上传、构建、等待和下载全流程。
5. Webhook 与业务事务不丢事件，失败重试后可成功或进入死信。
6. Webhook 签名可验证，重放和 SSRF 请求被拒绝。
7. CLI 在无交互 CI 环境中可完成标准流程且不泄露凭证。
8. GitHub/GitLab 只能访问授权仓库和 Artifact。
9. API Key 吊销和轮换立即生效且不影响已授权过渡 Key。
10. 限流、审计、错误码和 OpenAPI 文档一致。
11. 全量测试、CLI 测试、Webhook 故障测试和 Docker 重建通过。

只有全部验收有运行证据，V5 才能标记完成。
