# AppForge V1–V7 产品实施路线图

> 本文档把《APK 渠道动态打包平台方案》的产品演进路线转换为可执行的工程计划。总体产品方向以原方案为准，各阶段的功能边界、数据模型、接口、安全要求和验收标准以对应阶段实施规格为准。
>
> 项目级最新完成度、证据强度和阻塞项统一见 [V1–V7 项目完成度审计](./V1-V7项目完成度审计.md)。

## 1. 文档体系

| 阶段 | 实施规格 | 验收报告 | 核心目标 |
| --- | --- | --- | --- |
| V1 | [V1 功能闭环实施规格](./V1功能闭环实施规格.md) | 验收记录在规格第 14 节 | APK 上传、渠道注入、签名、分发与归因闭环 |
| V2 | [V2 基础白标实施规格](./V2基础白标实施规格.md) | [V2 基础白标验收报告](./V2基础白标验收报告.md) | AppName、Logo、启动图和 API Host |
| V3 | [V3 高级白标实施规格](./V3高级白标实施规格.md) | [V3 高级白标验收报告](./V3高级白标验收报告.md) | 动态包名、多签名策略和白标模板 |
| V4 | [V4 构建集群实施规格](./V4构建集群实施规格.md) | [V4 构建集群验收报告](./V4构建集群验收报告.md) | 多 Builder、并发、优先级和构建缓存 |
| V5 | [V5 开放平台实施规格](./V5开放平台实施规格.md) | [V5 开放平台验收报告](./V5开放平台验收报告.md) | Open API、Webhook、CI/CD 与代码平台集成 |
| V6 | [V6 商业化实施规格](./V6商业化实施规格.md) | [V6 商业化验收报告](./V6商业化验收报告.md) | 套餐、计费、构建/存储额度和用量 |
| V7 | [V7 企业交付实施规格](./V7企业交付实施规格.md) | [V7 企业交付验收报告](./V7企业交付验收报告.md) | Dedicated、私有部署和 Local Builder Agent |

## 2. 当前基线

截至 2026-08-17：

- V1 的 11 项验收标准已有运行证据，状态为完成。
- V2 基础白标的数据模型、预检、资源替换、构建快照、前端和 Docker 已实现；2026-08-15 在唯一临时 Compose 环境中完成公共管理 API → 真实 Worker 的不兼容 APK 负向 E2E，明确返回 Launcher/Splash 资源缺失、正式构建任务为 0，并验证临时数据、对象、净用量、容器、卷和网络全部清理。共享开发数据库未参与写入，严格验收为 12/12，阶段状态为完成。
- V3 模板、产品、声明式补丁、动态包名、证书绑定、敏感参数和真实可启动产物均已落地；Android 10（API 29）ARM64 Emulator 上的旧版安装、首次启动、UI、同签名原位升级和新版启动门禁已于 2026-08-17 使用当前脚本复验，详见 [V3 高级白标验收报告](./V3高级白标验收报告.md)。
- V4 多 Builder、公平调度、三级策略与节点槽位、取消/重试、租约恢复与 fencing、构建缓存、TTL/LRU、指标告警和管理页面已有完整历史验收；2026-08-15 清理约 2.435GB 可重建缓存后，三个节点经真实管理 API 恢复，当前只读验收再次全项通过，阶段状态为完成，详见 [V4 构建集群验收报告](./V4构建集群验收报告.md)。
- V5 Open API、API Key、Webhook、CLI/CI、GitHub/GitLab 受控集成、源码事件触发、开发者中心和独立 Worker 已完成，详见 [V5 开放平台验收报告](./V5开放平台验收报告.md)。开放平台继续复用现有 `appforge-ui` 与 `appforge-api`，通过 Agent 路由和 `/open/v1` 隔离，不重复新建前端/API 工程。
- V6 套餐版本、订阅权益、构建/存储/席位/API 额度、不可变用量账本、Stripe/人工合同、账单支付、商业化页面和独立 Worker 已完成，详见 [V6 商业化验收报告](./V6商业化验收报告.md)。
- V7 已完成 Compose/Helm 交付骨架、配置 Schema、迁移 Job、离线许可证、Local Agent 注册与 mTLS 控制面、客户侧安全安装/升级包、租户/应用授权、租约 fencing、企业 Secret Resolver、固定 APK 执行器，以及三种 Artifact 模式的合成真实构建闭环。`AIR_GAPPED` 已实测任务/额度锁定、双向 ECDSA 签名 ZIP、错误身份与篡改拒绝、`--network none` 构建、本地防重放、事务导入、幂等和最终 APK；平台/租户最小 RBAC 与租户路由已补齐，未授权 403 和专用角色显式授权通过。Schema 113 已提升为 1.2.x 生产默认，并通过空库安装、112→113、重复迁移、数据保持、已有角色零自动授权及本地断网全新安装。当前 Schema 113 的代表性租户/应用/版本/渠道/签名/任务/审计/AIR_GAPPED/六对象链已完成破坏恢复，RPO 2 秒、RTO 11 秒；同摘要 `1.2.0 → 1.2.1 → 1.2.0` kind/Helm 快速回归与正式签名 `v1.2.5 → v1.2.6 → v1.2.5` Kubernetes 升级/回滚均已通过。正式门禁在原生 `linux/amd64` 节点验证双版本聚合 Sigstore、21 个镜像实时 Cosign、9/9 个应用镜像摘要不同，升级 205 次与回滚 366 次连续 API 探测均成功、失败 0 次，并在回滚后保留 Schema 113、AIR_GAPPED 表和双 API 副本。客户存储模式已用受限临时 MinIO 实测最小前缀和控制面零客户字节；远程 APK 签名、生产安装/升级、NetworkPolicy、可观测性、灾备/PITR/对象副本、合成容量和 npm 官方零漏洞审计均已有本地证据。`v1.2.6` 正式 tag 已完成 22/22 发布任务，并通过公开 GitHub Release 匿名下载、双层 SHA/Sigstore 身份和 Linux CLI 可执行位验证；真实签名资产还生成了 SHA-256 为 `a88b792badcf39bd1522fd1e1923eb8226c0fa8f51e5ebb5cc5e9ecf9f46c306`、包含 18 个 `linux/amd64` 镜像的 3,324,800,000 字节正式离线介质，并通过本地断网全新安装。`v1.2.5 → v1.2.6 → v1.2.5` 正式介质升级/回滚也已在双 internal 网络下通过，两个 tag 的 Git commit 和 16/16 个 AppForge 平台镜像摘要均不同，数据库与对象标记保持。Android 授权真机、客户容量/峰值与小时/天级长稳、客户 Linux/物理断网介质及现场离线升级复验、客户 Kubernetes/CSI/Ingress/私有仓库、生产规模业务灾备/异地副本恢复/客户 RPO/RTO、真实 S3/OSS/AWS、客户 Prometheus/OTLP/SIEM 和客户真实 HSM 仍未完成，阶段状态保持“实施中”，详见 [V7 企业交付验收报告](./V7企业交付验收报告.md)。

2026-08-17 状态细化：其中“小时/天级长稳”已有小时级收敛——公开原生 `linux/amd64` 当前源码 Compose 完成 3600 秒认证 API、真实合成 APK 构建和临时 MinIO 对象往返并行门禁，三类负载错误均为 0，8 个核心容器零重启；天级、客户峰值及生产规模 APK/对象吞吐仍未完成，因此 V7 状态不变。证据见 `docs/enterprise/evidence/v7-api-soak-hour-public-ci-20260817.json` 与 `docs/enterprise/evidence/v7-artifact-mixed-soak-hour-public-ci-20260817.json`。

阶段状态只以对应规格中的验收证据为准，不以“已有字段”“已有页面”或“代码可以扩展”为完成依据。

## 3. 实施顺序

```text
V1 渠道闭环
  ↓
V2 基础白标
  ↓
V3 高级白标与模板
  ↓
V4 构建集群与缓存
  ↓
V5 开放平台
  ↓
V6 商业化与额度
  ↓
V7 企业交付
```

必须按顺序推进。允许提前保留兼容字段或接口，但不得以提前存在的局部能力跳过前一阶段验收。

## 4. 跨阶段工程约束

### 4.1 服务归属

- `services/system`：用户、租户、团队、RBAC、平台配置和平台审计。
- `services/core`：应用、品牌、模板、渠道、版本、归因、开放平台业务对象、套餐订阅和用量业务数据。
- `services/builder`：任务领取、构建执行、缓存、Builder 节点与 Local Agent 协议。
- `appforge-api`：管理端、代理端、公开端和开放 API 的 HTTP 边界。
- 跨服务只通过 RPC 使用数据，不重复创建业务表。

### 4.2 安全基线

所有阶段持续满足：

- JWT、API Key 或 Agent 凭证必须绑定租户和应用范围。
- 所有对象存储默认私有，下载使用短时授权。
- 密码、签名密钥、Webhook Secret、API Secret 不得明文存储或写入日志。
- 公开事件接口必须具备签名、防重放、限流和幂等。
- Builder 和内部 RPC 使用独立内部凭证，过期 Worker 不得回写。
- 租户隔离必须在 API、RPC、SQL 和对象 Key 四层同时成立。

### 4.3 阶段完成定义

每个阶段只有同时满足以下条件才可标记完成：

1. 规格中的数据表、枚举、API、RPC、页面和后台任务均已实现。
2. SQL、Proto 和生成代码按项目规则重新生成。
3. 单元测试、模块测试、前端类型检查和生产构建通过。
4. Docker 全量重建成功，长期服务运行正常。
5. 每项验收标准都有实际运行证据。
6. 文档、迁移、默认配置、权限种子和部署说明同步更新。
7. 不遗留会阻断下一阶段的临时兼容逻辑或明文敏感数据。

## 5. 里程碑输出

每个阶段输出：

- 对应阶段实施规格和验收记录。
- 数据库迁移与回滚说明。
- API/RPC 契约和调用示例。
- 管理端与代理端页面。
- 必要的 SDK、CLI、Agent 或示例工程。
- Docker 开发环境及生产部署配置模板。
- 安全检查、故障恢复和运维说明。

## 6. 状态维护规则

- `未开始`：只有规划，没有代码或运行证据。
- `实施中`：已有部分代码，但未通过全部验收。
- `完成`：全部验收有运行证据。
- 任何回归导致核心验收失败时，阶段状态恢复为 `实施中`。

2026-08-16：V7 新增 CONNECT-only 受限出口代理及 Compose/Helm 双层网络门禁，本地合成 TLS 已验证 Allowlist、明文拒绝、未登记目标拒绝和远程签名绕过；客户最终网络与外部端点仍待现场验收。

2026-08-16：正式发布证据供应链覆盖 16 个 AppForge 发布镜像与 Redis、Alpine 两个原始第三方运行镜像的 SPDX、许可证清单和 Trivy；AppForge 镜像另有逐镜像 Cosign 回验，Go/前端源码依赖使用不导出明细的 High/Critical 阻断，18 个 digest 汇总进 manifest/SHA/Sigstore 签名。`v1.2.6` 远端正式 tag 流水线 22/22 成功，公开 GitHub Release 四项资产已通过匿名下载、外层与内层签名/哈希及 Linux CLI 可执行位实测；由这些真实签名资产生成的 `linux/amd64` 正式离线介质完成 18 镜像加载、独立配置渲染和本地断网全新安装，证据见 `docs/enterprise/evidence/v7-public-release-v1.2.6-20260816.json` 与 `docs/enterprise/evidence/v7-formal-offline-media-v1.2.6-20260816.json`。

2026-08-17：新增正式双版本离线升级门禁，分别验证 `v1.2.5` 与 `v1.2.6` 发布身份、介质 SHA、Git commit、平台摘要和部署文件，在 `--pull never` 与双 internal 网络下完成 `1.2.5 → 1.2.6 → 1.2.5`；16/16 个 AppForge 镜像摘要不同，目标迁移镜像和 MySQL/etcd/MinIO 实际切换，数据库与对象标记保持，证据见 `docs/enterprise/evidence/v7-formal-offline-upgrade-v1.2.5-v1.2.6-20260817.json`。

2026-08-17：新增原生 `linux/amd64` 正式 Kubernetes 升级门禁，验证 `v1.2.5` 与 `v1.2.6` 聚合 Sigstore 身份并实时 Cosign 回验 21 个镜像；9/9 个 Helm 应用镜像摘要不同，kind v1.32.2 中完成 `1.2.5 → 1.2.6 → 1.2.5`，升级 205 次、回滚 366 次连续 API 探测均成功且失败 0 次，回滚后 Schema 113、AIR_GAPPED 表和双 API 副本保持。证据见 `docs/enterprise/evidence/v7-formal-kubernetes-upgrade-v1.2.5-v1.2.6-20260817.json`；客户 CSI/Ingress/私有仓库仍需现场复验。

2026-08-17：新增客户对象存储现场探针，正式 Local Agent 复用已登记 S3/MinIO/阿里云 OSS Secret，仅在登记前缀执行单个随机合成对象的写入、Stat、完整回读 SHA-256、删除和权威 NotFound 确认，不 List/读取既有对象或修改桶/策略。临时 MinIO 真实 Provider E2E 已验证既有对象不变、探针对象和 Docker 资源零残留及证据脱敏，证据见 `docs/enterprise/evidence/v7-customer-storage-probe-fixture-20260817.json`；真实客户 S3/OSS 尚待用 `environmentKind=customer-test` 运行，V7 状态不变。

2026-08-17：修复 Local Agent 二进制仍硬编码 `1.1.0` 的版本一致性问题。开发构建默认明确标记 `0.0.0-dev`，正式镜像从 tag 的 Docker metadata 注入语义化版本。公开原生 `linux/amd64` 客户存储探针 CI 已通过；`v1.2.7` 正式发布 22/22 作业成功，发布流水线和独立 digest 运行均逐字确认 `appforge-local-agent 1.2.7 protocol=3`，公开归档 SHA-256 清单复验通过。证据见 `docs/enterprise/evidence/v7-customer-storage-probe-release-v1.2.7-20260817.json`；真实客户 S3/OSS 门禁仍未关闭。

2026-08-17：补齐 SIEM RFC5424 over TLS 产品路径，复用已脱敏审计事件并以 RFC6587 八位计数帧发送，支持自定义 CA、TLS 1.2+、有界队列、64 KiB 上限、重连/退避及受限 HTTP CONNECT；明文 Syslog、危险 URL 和带凭据代理拒绝。真实回环 TLS/CONNECT、生产 Compose preflight 与 Helm 严格 lint 已通过；公开原生 `linux/amd64` Syslog、客户存储及正式 Kubernetes 三项回归也全部成功，证据见 `docs/enterprise/evidence/v7-syslog-tls-20260817.json` 与 `docs/enterprise/evidence/v7-syslog-tls-public-ci-20260817.json`。客户 SIEM 现场解析、路由、留存和告警联调仍待完成。

2026-08-17：新增认证 API 小时级长稳门禁。公开原生 `linux/amd64` Runner 从当前源码构建并启动全新 Compose 栈，连续 3600 秒探测 health、ready 和带 JWT/RBAC/RPC/数据库读取的部署状态；独立 P99、最小请求数、零错误及零连续错误门禁均通过，MySQL/Redis/etcd/MinIO/System/Core/Builder/API 8 个容器零重启，证据 Artifact 已上传且合成平台完成清理。证据见 `docs/enterprise/evidence/v7-api-soak-hour-public-ci-20260817.json`；天级、客户峰值和生产规模 APK/对象吞吐长稳仍未完成。

2026-08-17：小时级门禁进一步扩展为混合负载。公开原生 `linux/amd64` Runner 同时运行认证 API、当前源码 Local Agent 真实合成 APK 打包/签名和 1 MiB 临时 MinIO 对象 Put/Stat/Get/SHA/Delete/NotFound 往返；3600 秒内 API/构建/对象错误均为 0，至少完成 120 次 APK 构建和 600 次对象往返（每方向至少 600 MiB），8 个核心容器零重启，证据 Artifact 上传和合成资源清理成功。证据见 `docs/enterprise/evidence/v7-artifact-mixed-soak-hour-public-ci-20260817.json`；该结果不代表天级、客户峰值或生产规模容量。

2026-08-17：增强 V3 Android 运行脚本的机器证据输出，并在无快照只读 API 29 ARM64 AVD 上用既有合成任务 14→15 重新通过旧版安装/启动/UI、同签名 `adb install -r` 原位升级、新版启动/UI 和 `firstInstallTime` 保持门禁。0600 证据见 `docs/enterprise/evidence/v3-android-runtime-current-20260817.json`；该 Emulator 结果不替代 V7 的客户授权物理真机门禁，V7 状态不变。

当前状态：V1、V2、V3、V4、V5、V6 完成；V7 实施中。V7 的 13 条严格验收全部具备实际运行和交付证据后，才可标记 V1–V7 总目标完成。
