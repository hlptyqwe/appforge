# AppForge V4 构建集群实施规格

## 1. 目标

V4 将 Builder 从可横向运行的 V1 Worker 提升为可运营的构建集群：

- 多 Builder 节点注册与心跳。
- 租户和套餐驱动的并发控制。
- 任务优先级与公平调度。
- 可验证的构建缓存。
- 节点隔离、容量管理和运维可观测性。

## 2. 当前实现

截至 2026-08-14，V4 已完成：

- Builder 节点注册、心跳、容量、工具链、磁盘和健康状态。
- 全局、租户、应用和节点四级并发槽位控制。
- 租户加权公平队列、租户内优先级和最高优先级限制。
- `builder_id + builder_attempt + lease` fencing、过期恢复和旧 Worker 回写拒绝。
- 输入可寻址租户缓存、下载后 SHA/大小确认、损坏降级、TTL/LRU 和手工失效。
- 节点 Drain、任务取消/重试、结构化调度事件和集群指标/告警摘要。
- 三个 Docker Worker 的并行构建、故障恢复和跨租户隔离验收。

## 3. 数据模型

### 3.1 Builder 节点 `t_builder_node`

```text
id, node_code, pool_code, endpoint
status, drain_status, max_concurrency, running_count
cpu_capacity, memory_capacity, disk_capacity
toolchain_version, capability_json
last_heartbeat_at, create_time, update_time
```

### 3.2 调度租约 `t_build_slot_lease`

记录 tenant/app/pool 的并发槽位，和任务租约在同一事务中获取、释放或恢复。

### 3.3 构建缓存 `t_build_cache_entry`

```text
id, tenant_id, cache_scope, cache_key
toolchain_version, input_digest, artifact_object_id
artifact_sha256, size_bytes, hit_count
status, expires_at, last_hit_at, create_time
```

`cache_key` 由所有影响产物的不可变输入生成，包括：源 APK SHA、渠道/品牌/模板快照、签名证书指纹、工具链版本和构建协议版本。

### 3.4 调度事件 `t_build_scheduler_event`

保存领取、排队、限流、恢复、排空、取消和缓存命中的结构化事件，用于运维排查。

## 4. 调度规则

- 优先级只在同一公平队列权重内生效。
- 平台保留紧急优先级，普通租户不能自行设置无限高值。
- 同租户并发、同应用并发和全局并发分别限制。
- 节点 capability 必须满足任务所需工具链和模板协议。
- Drain 节点不领取新任务，但允许已有任务完成。
- 取消任务通过状态和 fencing 生效，不能只杀进程。

## 5. 缓存规则

- 缓存默认租户隔离，不跨租户复用签名后 APK。
- 未签名中间产物只有在不包含租户 secret 时才允许受控共享。
- 命中缓存前重新校验对象存在、大小、SHA 和工具链版本。
- 缓存命中仍创建独立任务记录和产物引用，审计不能被跳过。
- 缓存条目支持 TTL、容量上限、LRU 清理和手工失效。
- 签名配置、模板修订或工具链升级自动改变 cache key。

## 6. API/RPC

内部 Builder 协议增加：

- RegisterBuilderNode
- BuilderNodeHeartbeat
- ClaimScheduledBuildTask
- DrainBuilderNode
- CancelBuildExecution
- ResolveBuildCache
- PublishBuildCache

管理 API 增加：

```text
GET  /admin/core/build-cluster/metrics
GET  /admin/core/build-cluster/nodes
POST /admin/core/build-cluster/nodes/:id/drain
GET  /admin/core/build-cluster/policies
POST /admin/core/build-cluster/policies
GET  /admin/core/build-cluster/cache
POST /admin/core/build-cluster/cache/:id/invalidate
POST /admin/core/build-cluster/cache/cleanup
GET  /admin/core/build-cluster/events
POST /admin/core/build-tasks/:id/cancel
POST /admin/core/build-tasks/:id/retry
```

管理端和代理端共用 `appforge-api` 的 HTTP 边界；V4 集群运维接口只开放给管理端，不单独新建 API 项目。

## 7. Worker 并发模型

- 单 Worker 进程支持配置化并发槽位，不为每个任务无限创建 goroutine。
- 每个执行单元拥有独立临时目录、日志和取消上下文。
- 节点退出时先 Drain，再等待任务完成；超时后取消并由租约恢复。
- 心跳包含当前任务、磁盘余量和工具链版本。
- 磁盘不足、工具链不一致或连续失败节点自动隔离。
- 品牌兼容性预检与正式构建共享节点可调度边界：隔离、Drain、心跳过期、并发已满、低磁盘或未声明 `branding` capability 的节点均不得领取预检。

## 8. 可观测性

至少提供：

- 排队时长、构建时长、成功率和失败分类。
- 各节点运行数、容量、心跳延迟和磁盘余量。
- 缓存命中率、节省时间和占用空间。
- 租约恢复次数、fencing 拒绝次数和取消耗时。
- tenant/app/pool 维度指标与告警。

当前由 `GetBuildClusterMetrics` 聚合统计窗口内成功率、排队/构建时长、缓存命中、租约恢复和缓存校验失败，并结合节点、槽位、磁盘与队列状态生成结构化告警代码。原始调度依据保存在 `t_build_scheduler_event`，可在集群页面按任务、节点和事件类型追溯。

## 9. 前端

- Builder 集群看板。
- 节点详情、Drain/恢复操作。
- 任务排队位置、取消和重试。
- 缓存列表、命中详情、失效和容量统计。
- 并发和优先级策略配置。
- 成功率、排队/构建时长、缓存命中、容量和告警概览。

## 10. 实施顺序

1. 节点注册、心跳和能力模型。
2. 公平调度、并发槽位、取消与 Drain。
3. 可寻址缓存、校验和清理。
4. 指标、告警和运维页面。
5. 多节点故障、滚动升级和压力验收。

## 11. 验收标准

1. 三个 Builder 节点可并行构建且不重复领取同一 attempt。
2. 租户/应用/全局并发限制都能实际阻止超额执行。
3. 高优先级任务优先执行，同时普通租户不会永久饥饿。
4. Drain 节点不领取新任务，已有任务可平滑完成。
5. 任务取消后旧 Worker 不能上传或完成结果。
6. 相同不可变输入第二次构建命中缓存，不同快照或工具链不命中。
7. 缓存对象损坏时自动失效并执行真实构建。
8. TTL/LRU 清理不删除仍被有效任务引用的对象。
9. 节点中断后租约、槽位和任务均可恢复。
10. 压力测试下没有跨租户数据、缓存或签名产物泄漏。
11. 集群指标、日志和审计能定位每次调度决定。
12. 全量测试和多 Worker Docker 验收通过。

只有全部验收有运行证据，V4 才能标记完成。

## 12. 验收结果

2026-08-14，以上 12 项标准均已获得运行证据。2026-08-15 当前环境曾因 Docker VM 低磁盘触发节点隔离；清理可重建缓存后，三个固定节点已通过真实管理 API 恢复，当前 12 项只读验收再次通过，阶段状态为 `完成`。品牌预检领取已补齐同一节点门禁并随 Core 镜像部署，单测覆盖隔离/Drain/心跳/并发/磁盘/能力拒绝，当前运行库合格 branding 节点计数为 3。历史与当前证据详见 [V4 构建集群验收报告](./V4构建集群验收报告.md)。
