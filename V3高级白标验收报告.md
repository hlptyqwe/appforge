# AppForge V3 高级白标验收报告

## 1. 当前结论

验收日期：2026-08-14。

V3 的模型、RPC/API、前端、Builder、权限、审计、安全校验和 Docker 部署已经落地。真实 APK 已包含 `classes.dex`、MAIN/LAUNCHER Activity 和 Provider，实现动态包名后组件类名保持、Provider authority、OAuth scheme、App Links、扩展文件、独立签名和同证书升级产物。

Android 10（API 29）ARM64 Emulator 上的旧版安装、首次启动、同包名覆盖升级和新版首次启动均已通过。V3 的 10 项验收标准已有运行证据，当前状态为 `完成`。

## 2. 关键构建证据

### 2.1 可启动旧版产物

- 构建任务：`14`，状态 `SUCCESS`。
- 白标产品：`7`。
- 目标包名：`com.appforge.v3runtime20260814`。
- APK 对象：`47`。
- APK SHA-256：`41a32ac19b9aa9fb7a6ed07e5111931c11d8dd381b234a7a20d7670a00706046`。
- 任务、源 APK 和产物的版本均为 versionCode `20260820`、versionName `3.0.0-v3-runtime`。
- 启动 Activity：`com.appforge.acceptance.MainActivity`。
- APK 包含 `classes.dex`、`assets/appforge/template.json` 和 `assets/appforge/customer-runtime.json`。

### 2.2 同包名升级产物

- 构建任务：`15`，状态 `SUCCESS`。
- 白标产品：`7`，与旧版使用同一产品、包名和签名配置。
- APK 对象：`49`。
- APK SHA-256：`4a77f04ee410775a9dbd6a7bbcf7b8239be467cd1814759072bb79d0ab85fb78`。
- 任务、源 APK 和产物的版本均为 versionCode `20260821`、versionName `3.0.1-v3-runtime`，高于旧版。
- 启动 Activity：`com.appforge.acceptance.MainActivity`。
- v1、v2、v3 APK 签名验证全部通过。
- 证书 SHA-256：`59133f9c597d6968f37195a6b39330c0bef61a96be5faa7b98bedf0f60170d82`。

### 2.3 Manifest 与模板结果

任务 14/15 的产物已由 `aapt` 和 `apksigner` 验证：

- Manifest package 已变更为白标产品包名。
- Permission 名称已变更为 `com.appforge.v3runtime20260814.permission.C2D_MESSAGE`。
- 相对组件 `.MainActivity` 和 `.SmokeProvider` 已转换并保持为源 DEX 中的绝对类名，未被错误改到目标包路径。
- Provider authority 为 `com.appforge.v3runtime20260814.provider`。
- OAuth scheme 为 `appforgeruntime`。
- App Link host 为 `runtime.customer.example.com`。
- AppName 为 `AppForge 白标验收`，Logo、启动图和 API Host 继续使用品牌快照。

## 3. 其他验收证据

1. 模板发布使用事务和行锁，发布修订不可原地修改，历史任务保存 revision 与 checksum 快照。
2. 操作协议采用固定字段 Schema；路径穿越、未知字段、任意脚本、未声明参数引用和危险明文 secret 均被测试拒绝。
3. packageName 绑定证书 SHA-256；同包名错误证书在预检/建任务前被拒绝，绑定记录保存首个和最近构建任务。
4. 任务 `7`、`8` 验证两个产品并行构建和数据隔离；任务 `9` 验证 `resource.replaceFile`；任务 `10` 验证 Provider、OAuth 与 App Links；任务 `11` 验证敏感参数加密、API 掩码和 Builder 受信阶段解密。
5. 敏感参数数据库仅保存 `sb1.*` 密文，查询和任务快照 API 返回 `***`，审计记录整体脱敏且不含明文或密文。
6. Core、Builder、API Go 测试通过，前端类型检查和生产构建通过。
7. `builder-worker` 已用修复后的镜像重建；AppForge 长期 Docker 服务运行正常。

## 4. Android 设备门禁

仓库提供 [v3-android-runtime.sh](./deploy/acceptance/v3-android-runtime.sh)，脚本会：

1. 确认只连接一台已授权 Android 设备。
2. 仅卸载验收包 `com.appforge.v3runtime20260814`。
3. 安装旧版并启动 MAIN Activity，确认进程存在。
4. 使用 `adb install -r` 覆盖安装新版。
5. 再次启动，校验版本递增、首次安装时间保持、包名和签名升级约束成立。

执行命令：

```bash
ANDROID_SDK_ROOT=/Users/sky/Library/Android/sdk \
  ./deploy/acceptance/v3-android-runtime.sh \
  /private/tmp/appforge-v3-runtime-final-old.apk \
  /private/tmp/appforge-v3-runtime-final-new.apk \
  com.appforge.v3runtime20260814 \
  com.appforge.acceptance.MainActivity \
  20260820 \
  20260821 \
  59133f9c597d6968f37195a6b39330c0bef61a96be5faa7b98bedf0f60170d82
```

实际执行环境：

- AVD：`appforge_v3_api29`。
- Android：`10`（API 29）。
- ABI：`arm64-v8a`。
- 设备：`emulator-5554`。

实际执行结果：

- 旧版安装返回 `Success`，Activity 冷启动返回 `Status: ok`。
- 新版 `adb install -r` 返回 `Success`，覆盖升级后 Activity 再次冷启动返回 `Status: ok`。
- 设备最终 versionCode 为 `20260821`，minSdk 为 `21`，targetSdk 为 `29`。
- `firstInstallTime` 在覆盖升级前后保持为 `2026-08-14 16:48:49`，证明不是卸载重装。
- 前台组件为 `com.appforge.v3runtime20260814/com.appforge.acceptance.MainActivity`。
- UIAutomator 读取到界面文本 `AppForge V3 Runtime Smoke OK`，应用进程持续存在。

至此，V3 验收标准 8、9 的设备运行证据已闭环，可以按路线进入 V4。
