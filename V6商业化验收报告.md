# AppForge V6 商业化验收报告

验收日期：2026-08-14

## 1. 结论

V6 商业化能力已完成。套餐版本、订阅与权益快照、不可变用量账本、构建/并发/存储/席位/API 额度、账单支付、Stripe 回调、人工合同、订阅变更、用量与账单页面均已落地，并通过并发运行时测试、全量测试、前端生产构建和 Docker 全量重建。

生产环境仍须通过 Secret Manager 注入 Stripe Secret Key 与 Webhook Secret；开发 Compose 故意不配置可发起真实扣款的 Secret Key，避免误调用外部支付。该项属于部署密钥配置，不是代码能力缺口。

## 2. 实现范围

- `services/core` 归属 10 张商业化业务表，提供套餐、订阅、权益、额度、用量、账单和支付 RPC。
- 套餐采用不可变版本；现有租户迁移到 Free 套餐，新租户首次使用时自动建立 Free 订阅和权益。
- 构建任务创建与额度预占处于同一事务；确认、释放和用量入账均以幂等键保护。
- 构建领取再次校验订阅状态和租户并发权益；缓存命中、失败、重试按套餐规则计量或冲正。
- 源 APK、构建产物和活跃席位进入存储/席位用量；历史现有数据由迁移追加回填账。
- Open API 的最终速率取 API Key 配额与套餐权益的较小值，每次请求写入幂等用量。
- 支持人工合同，以及 Stripe Checkout、支付成功/失败、退款、争议、续期、周期末取消和宽限暂停。
- Stripe 回调先基于原始请求体验签，再解析业务字段；事件唯一、乱序保护，原始 payload 加密并设置保留期。
- 管理端提供套餐版本、租户订阅和人工合同操作；代理端提供套餐对比、订阅、用量、阈值和发票列表。
- 独立 `billing-worker` 负责预占过期、订阅续期/降级/取消/宽限和支付事件密文过期处理。

## 3. 运行证据

### 3.1 并发额度与计量

`TestBillingRuntimeAcceptance` 使用独立测试租户，将周期构建额度设置为 5 后并发提交 20 次预占：仅 5 次成功，15 次稳定返回额度耗尽，未发生超卖。随后对成功预占重复确认，账本仍只有 5 条；70%、90%、100% 三个阈值各产生一条通知。

### 3.2 支付幂等与财务状态

同一运行时测试覆盖：

- 已有 Free 订阅升级绑定 Stripe 外部订阅。
- `invoice.paid` 重复事件只生成一张发票和一笔支付事实。
- 更早到达的删除事件不会回退较新的有效订阅状态。
- 退款和争议分别形成独立支付流水，退款金额使用整数最小货币单位。
- Webhook 原始内容以 `sb1.` 密文保存，不出现业务明文。

API Handler 测试另行覆盖 Stripe HMAC-SHA256 签名、5 分钟时间容差、过期签名拒绝和标准事件归一化。

### 3.3 数据库与部署

- `20260814_106_v6_commercialization` 与 `20260814_107_v6_billing_menu` 已执行。
- 10 张 V6 业务表存在；Free、Pro、Business 三个默认有效套餐存在。
- 当前全部租户均有且仅有一份订阅和权益快照。
- `appforge-billing-worker` 正常运行；API、Core RPC、管理端和代理端均健康。
- `docker compose -f deploy/docker-compose.dev.yml up -d --build` 已成功完成全量镜像重建和部署。

## 4. 验证命令

```bash
cd /Users/sky/local/go/src/appforge
./deploy/acceptance/v6-commercialization.sh
```

已通过：

- 10 个 Go Module 的 `go test ./...`。
- V6 MySQL 运行时并发与支付验收测试。
- `appforge-ui` 的 `npm run type-check`。
- `appforge-ui` 的 `npm run build`。
- Docker 服务健康检查与 V6 迁移/种子/租户回填检查。

## 5. 验收项映射

1. 套餐不可变版本保留历史订阅和账单事实：通过。
2. 构建、并发、存储、席位和 API 额度有事务预占或幂等账本保护：通过。
3. 用量重复确认不重复计费，调整通过追加账完成：通过。
4. 70%、90%、100% 阈值通知幂等，超额返回稳定 gRPC 额度错误：通过。
5. 升级、降级、周期末取消、人工合同、宽限和恢复状态机：通过。
6. 支付成功、失败、退款、争议、重复和乱序回调：通过。
7. 金额全部采用整数最小货币单位，账单项与支付流水不可变：通过。
8. 缓存命中、失败构建和重试由套餐策略控制计量：通过。
9. 代理端请求由 JWT 租户上下文限定，管理端全租户查询受 RBAC 控制：通过。
10. 支付和额度操作不修改已有 APK、构建任务与归因历史：通过。
11. 全量测试、并发额度测试、签名支付沙箱和 Docker 重建：通过。

## 6. 可重复验收

[V6 验收脚本](./deploy/acceptance/v6-commercialization.sh) 会检查容器、迁移、数据模型、套餐、租户回填、唯一约束和金额类型，并执行 Stripe 验签测试及真实 MySQL 并发额度/支付事件测试。测试数据使用随机租户并在结束时清理。
