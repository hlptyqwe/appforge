# AppForge V7 企业交付实施规格

## 1. 目标

V7 将 AppForge 从 SaaS 产品扩展为可重复交付、可升级、可审计的企业方案：

- Dedicated 独立实例。
- 企业 Private Deployment。
- 部署在客户网络中的 Local Builder Agent。
- 离线/受限网络、企业 Secret、备份恢复和升级治理。

## 2. 交付模式

### 2.1 SaaS

平台托管，多租户共享控制面和 Builder 池，通过租户隔离与套餐控制资源。

### 2.2 Dedicated

平台托管但实例、数据库、对象存储和 Builder 池为单客户独占。控制面仍由平台运维。

### 2.3 Private Deployment

控制面、数据库、对象存储和 Builder 全部部署在客户环境。支持在线和离线两种安装介质。

### 2.4 Hybrid + Local Builder Agent

控制面可在 SaaS/Dedicated，APK、Keystore 和构建过程留在客户网络；控制面只保存授权元数据和脱敏状态。

## 3. 部署包

正式交付至少提供：

- Docker Compose 单机参考部署。
- Kubernetes Helm Chart。
- values Schema 与生产配置检查器。
- 镜像清单、SBOM、签名和漏洞报告。
- 数据库迁移 Job 和版本兼容矩阵。
- 安装、升级、回滚、备份、恢复和故障排查手册。
- 在线镜像仓库和离线 OCI 镜像包。

开发环境默认凭据不得进入生产模板。

## 4. Local Builder Agent

### 4.1 信任模型

- Agent 由租户管理员在控制面创建一次性注册码。
- 首次注册后签发短期客户端证书，后续使用 mTLS。
- Agent 凭证绑定 tenant、agentId、能力和允许的应用。
- 控制面不能用其他租户任务调度到该 Agent。
- Agent 支持吊销、证书轮换和远程 Drain，但不允许控制面执行任意命令。

### 4.2 数据模型

新增：

- `t_local_agent`
- `t_local_agent_registration`
- `t_local_agent_certificate`
- `t_local_agent_capability`
- `t_hybrid_artifact_reference`

证书私钥只存在 Agent 本地，服务端只保存证书和吊销状态。

### 4.3 通信协议

Agent 主动建立出站连接，避免要求客户开放入站端口：

```text
Register
Heartbeat
Claim/ReceiveTask
RenewLease
ReportProgress
Complete/Fail
RotateCertificate
Drain
```

协议使用 gRPC mTLS 或等价双向认证通道，消息包含 tenant、agent、task、attempt、timestamp 和 nonce。仍沿用 V4 fencing。

### 4.4 Artifact 模式

支持：

1. `CONTROL_PLANE_STORAGE`：与 SaaS 一样使用控制面对象存储。
2. `CUSTOMER_STORAGE`：Agent 访问客户 S3/MinIO/OSS，控制面只保存对象引用和 SHA。
3. `AIR_GAPPED`：任务和产物通过受签名离线包导入导出。

任何模式都必须验证大小、SHA、租户和任务 attempt。

Task Bundle、短时传输票据、幂等和三种模式的数据边界详见 [Local Agent Task Bundle 与 Artifact 协议](./docs/enterprise/local-agent-artifact-protocol.md)。

## 5. 企业 Secret

提供可插拔 Secret Provider：

- Kubernetes Secret。
- HashiCorp Vault。
- AWS KMS/Secrets Manager。
- 客户 HSM/远程签名接口。
- Local Agent 本地 Secret Store。

Builder 只在任务执行期间获取最小权限 Secret；日志、数据库和控制面事件不返回明文。

## 6. 高可用与灾备

- API/RPC 无状态多副本。
- MySQL 主从/托管高可用和时间点恢复。
- Redis/etcd 高可用配置。
- 对象存储版本化、生命周期和跨区域备份策略。
- Builder 可跨节点恢复，Local Agent 离线不丢任务。
- 明确 RPO/RTO，并通过演练验证。

## 7. 升级与兼容

- 使用语义化产品版本和数据库 schema version。
- 升级前执行 preflight：容量、备份、版本跨度和废弃配置。
- 数据库迁移前向兼容至少一个滚动升级窗口。
- Agent 与控制面维护协议兼容矩阵，旧 Agent 超出窗口时拒绝新任务但允许安全升级。
- 支持有状态回滚边界说明，不承诺不可逆迁移后的伪回滚。

## 8. 企业安全

- 镜像签名、SBOM、依赖漏洞和许可证清单。
- mTLS、客户 CA、自定义域名和证书轮换。
- 网络策略、出站域名白名单和代理配置。
- 审计日志导出到 SIEM。
- 数据保留、删除和导出策略。
- 可选 SSO/SAML/OIDC 和 SCIM；若启用必须映射租户与角色。
- 安全配置基线检查和最小权限运行用户。

## 9. 运维与支持

- `/healthz`、`/readyz` 和依赖健康检查。
- Prometheus 指标、结构化日志和 Trace 导出。
- 诊断包只收集脱敏配置、状态和最近错误摘要。
- License/Entitlement 支持离线签名许可证，不依赖修改系统时间。
- 支持工单所需版本、部署模式、组件状态和迁移版本信息。

## 10. 前端

平台端：

- Dedicated/Private 实例、版本和许可证管理。
- Agent 注册、证书、能力、状态和 Drain。
- 企业部署健康、升级与诊断状态。

租户端：

- Local Agent 安装向导和一次性注册码。
- Agent 应用授权、存储模式和构建池选择。
- 证书到期、离线和能力不匹配告警。
- AIR_GAPPED 任务锁定导出、短时下载、状态查询、结果 ZIP 私有上传和签名导入；按钮分别受 export/view/import 权限控制。

## 11. 实施顺序

1. 生产配置 Schema、Helm、镜像签名和迁移 Job。
2. Dedicated/Private 配置分层和备份恢复。
3. Local Agent 注册、mTLS、能力和调度。
4. 客户存储、企业 Secret 和离线模式。
5. SSO/SIEM、升级治理、诊断和交付验收。

## 12. 验收标准

1. 全新 Dedicated 和 Private 环境可按文档一次安装成功，无开发默认密钥。
2. Helm 滚动升级期间 API 可用，迁移与版本兼容符合矩阵。
3. 备份恢复到独立环境后，租户、任务、对象引用和审计一致。
4. Local Agent 只使用出站连接即可注册和构建。
5. Agent mTLS、租户/应用范围、吊销和证书轮换实际生效。
6. 控制面不能向 Agent 下发任意命令或其他租户任务。
7. 三种 Artifact 模式均校验 SHA、任务 attempt 和数据归属。
8. Agent 中断、升级和 Drain 后任务可安全恢复，旧进程不能回写。
9. Vault/KMS/本地 Secret 至少各完成一种真实集成验收，明文不进入控制面。
10. 离线安装、离线升级和离线许可证在断网环境可运行。
11. 镜像签名、SBOM、漏洞门禁、网络策略和 SIEM 导出通过安全验收。
12. RPO/RTO 灾备演练和升级回滚演练有记录。
13. 全量功能、性能、安全、部署和升级测试通过。

只有全部验收有实际运行和交付证据，V7 及 V1–V7 总目标才能标记完成。

## 13. 当前实施状态

截至 2026-08-17，V7 处于“实施中”。企业部署、Local Agent 安全控制面、客户安装升级包、固定 APK 执行器、企业 Secret Resolver、离线许可证、诊断、部署状态、可观测性、受限出口、灾备/PITR/对象副本与本地容量门禁已有实现和合成证据。`CONTROL_PLANE_STORAGE`、`CUSTOMER_STORAGE`、`AIR_GAPPED` 三种 Artifact 模式均已使用合成 APK/Keystore 完成真实构建 E2E；AIR_GAPPED 还覆盖平台/租户细粒度 RBAC、未授权 403、显式专用角色授权、双向签名、防篡改、防重放、`--network none` 和事务导入。Schema 113 已提升为 1.2.x 生产默认，生产 Compose/Helm/API/Release 已同步，并通过空库安装、112→113 升级、重复迁移、数据保持、已有角色零自动授权及本地断网全新安装；当前生产边界还完成了包含 AIR_GAPPED 与六对象链的破坏恢复、同摘要 `1.2.0 → 1.2.1 → 1.2.0` kind/Helm 快速回归，以及原生 `linux/amd64` 上正式签名 `v1.2.5 → v1.2.6 → v1.2.5` Kubernetes 升级/回滚。正式门禁验证双版本聚合 Sigstore、21 个镜像实时 Cosign 和 9/9 个不同应用镜像摘要，升级 205 次与回滚 366 次连续探测全部成功且失败 0 次，回滚后 Schema 113、AIR_GAPPED 表和双 API 副本保持。发布工作流已补齐 16 个 AppForge 发布镜像与 Redis、Alpine 两个原始第三方运行镜像的 SPDX、许可证清单和 Trivy，AppForge 镜像另有逐镜像 Cosign 回验；Go/前端源码依赖使用不导出明细的 Trivy 文件系统 High/Critical 门禁，npm 官方生产及全部依赖审计为零漏洞。全部 18 个镜像 digest、证据和源码门禁状态纳入聚合清单与签名，离线介质强制验证并携带该证据。`v1.2.6` 正式 tag 已完成 22/22 任务，并通过公开 Release 匿名下载、外层与内层 SHA/Sigstore 身份和四个 Linux CLI 可执行位验证；由真实签名资产生成的 3,324,800,000 字节 `linux/amd64` 正式离线介质已通过 SHA 校验、18 镜像加载、独立配置渲染和本地断网全新安装。`v1.2.5 → v1.2.6 → v1.2.5` 还使用两个正式签名介质完成本地禁拉取升级/回滚：两个 Git commit 和 16/16 个 AppForge 平台摘要不同，目标迁移镜像、应用及 MySQL/etcd/MinIO 实际切换，数据库与对象标记保持。客户 Linux/物理断网介质交接与现场升级、客户 Kubernetes/CSI/Ingress/私有仓库、真实 S3/OSS/AWS、生产规模灾备/真实跨区域恢复、客户 RPO/RTO、客户 Prometheus/OTLP/SIEM、客户容量与长稳、Android 授权真机和真实 HSM 仍未闭环。逐项证据和剩余工作以 [V7 企业交付验收报告](./V7企业交付验收报告.md) 为准；不得仅依据接口、枚举、脚本或页面存在而标记完成。

2026-08-16 补充：受限网络出口已新增 HTTPS CONNECT-only 代理、域名端口 Allowlist、Compose internal/egress 双网络隔离、Helm 代理专属 NetworkPolicy 和生产预检。本地合成 TLS 已验证允许目标、未登记目标与明文拒绝，并确认远程 APK 签名不继承代理。客户最终域名/IP、CNI、DNS、防火墙、代理容量和真实 SIEM/OTLP 等现场联调仍属于外部验收，V7 状态不变。

2026-08-17 补充：Local Agent 客户交付包新增 S3/MinIO/阿里云 OSS 现场探针，复用已登记 `local-file://` Secret 和真实 Provider，仅在登记前缀创建单个随机合成对象，完成 Put、Stat、完整 Get/SHA-256、Delete 与权威 NotFound 确认；禁止 List、读取既有对象、创建桶或修改策略，证据只保存目标指纹。临时 MinIO 夹具已验证既有对象不变、探针对象与 Docker 资源零残留；真实 S3/OSS 仍须客户测试环境执行，V7 状态不变。

2026-08-17 补充：Local Agent 产品版本不再硬编码，Docker 构建通过受语义化格式约束的 `APPFORGE_VERSION` 与 Go `-X main.version` 注入；正式发布工作流从 tag metadata 传值，并在发布后实际运行 digest 镜像核对版本与协议。公开原生 `linux/amd64` 客户存储探针 CI 已通过，`v1.2.7` 正式发布 22/22 作业成功，正式 digest 镜像由工作流与独立运行共同确认输出 `appforge-local-agent 1.2.7 protocol=3`，公开发布归档 SHA-256 清单也已通过。证据见 `docs/enterprise/evidence/v7-customer-storage-probe-release-v1.2.7-20260817.json`；该证据不替代真实客户 S3/OSS 现场探针。

2026-08-17 补充：SIEM 审计导出补齐 `syslog+tls://`，按 RFC5424 生成 `local0.info` 审计消息，使用 RFC6587 八位计数帧、TLS 1.2+、自定义 CA、64 KiB 上限、有界队列、超时与有限指数重试；每次重试重新建连，启用受限出口时通过无凭据 HTTP CONNECT。明文 Syslog、URL 凭据、路径/查询和缺失端口均拒绝。真实回环 TLS/CONNECT、Compose preflight 与 Helm 严格 lint 已通过；公开原生 `linux/amd64` Syslog 门禁及其触发的客户存储、正式 Kubernetes 回归也全部成功，证据见 `docs/enterprise/evidence/v7-syslog-tls-20260817.json` 与 `docs/enterprise/evidence/v7-syslog-tls-public-ci-20260817.json`。客户 SIEM 解析/路由/留存/告警仍属现场门禁，V7 状态不变。

2026-08-17 补充：新增 `deploy/acceptance/v7-api-soak.sh` 与公开 `.github/workflows/hourly-api-soak.yml`。脚本对 health、ready、带 JWT/RBAC/RPC/数据库读取的部署状态设置独立并发、间隔、P99、最小请求数、总错误及连续错误门禁，密码只接受私有 0600 文件，报告不记录 URL 原值。公开原生 `linux/amd64` Runner 使用当前源码构建全新 Compose 栈并连续运行 3600 秒，三个探针零错误、零连续错误且满足延迟/请求门禁，8 个核心容器零重启，Artifact 上传和合成资源清理均成功；证据见 `docs/enterprise/evidence/v7-api-soak-hour-public-ci-20260817.json`。该结果只关闭合成认证 API 的小时级门禁，天级、客户峰值、APK 构建与对象吞吐长稳仍属未完成项，V7 严格状态仍为 6 条通过、7 条部分通过。

2026-08-17 补充：新增 `deploy/acceptance/v7-artifact-soak.sh`，将小时级门禁扩展为认证 API、当前源码 Local Agent 合成 APK 打包/签名及临时 MinIO 对象往返并行负载。MinIO 凭据只接受私有普通文件，报告仅保存 Endpoint 指纹；每次对象操作均执行 Put、Stat、完整 Get/SHA-256、Delete 和权威 NotFound，最后清理唯一临时前缀。公开原生 `linux/amd64` 运行连续 3600 秒，三类负载错误均为 0，至少完成 120 次 APK 构建、600 次 1 MiB 对象往返和每方向 600 MiB，对象/凭据/平台均完成清理，8 个核心容器零重启。证据见 `docs/enterprise/evidence/v7-artifact-mixed-soak-hour-public-ci-20260817.json`；该合成证据不替代天级、客户峰值、生产规模容量或 Android 授权真机，V7 严格状态不变。

2026-08-17 补充：小时混合门禁进一步提升为代表性尺寸载荷。固定 Local Agent 执行器可为合成 APK 注入 0–256 MiB 随机不可压缩资产，并逐字节验证该资产在源 APK 与最终签名 APK 中的 SHA-256 保持；危险尺寸在启动 Docker 前拒绝。公开原生 `ubuntu-24.04` Runner 使用当前提交 `12df402ea2be04cc0fd8f47c5ca51c8b88b738a3` 连续运行同一 Compose 栈 3600 秒，工作流强制下限为三类认证 API 至少 32,400 次、100 个 32 MiB 合成 APK（至少 3,355,443,200 字节源载荷）及 600 次 8 MiB 对象完整往返（每方向至少 5,033,164,800 字节）；错误为 0，8 个核心容器零重启，证据上传和合成平台清理通过。公开证据见 `docs/enterprise/evidence/v7-representative-artifact-soak-hour-public-ci-20260817.json`。该结果仍是单 Runner 合成小时证据，不等于天级、客户峰值或生产容量，V7 严格状态保持 6 条通过、7 条部分通过。
