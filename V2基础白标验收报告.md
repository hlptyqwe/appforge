# AppForge V2 基础白标验收报告

## 1. 当前结论

复核日期：2026-08-15。

V2 的数据模型、RPC/API、品牌配置前端、图片私有对象、兼容性预检、不可变构建快照和 Builder 资源替换均已实现。既有合成任务 `4` 的源 APK、Logo、启动图和产物仍存在于本地私有 MinIO，数据库元数据与逐字节 SHA-256 一致；产物通过 `apksigner`、`zipalign`、`aapt`、`apktool` 和内嵌快照复核。

严格按 12 条验收标准计，当前为 `12 条通过`，阶段状态为 `完成`。第 3 条已经在唯一临时 Compose 环境中通过公共管理 API 创建不兼容合成 APK、品牌图片、版本和预检，真实 Worker 返回 `INCOMPATIBLE` 及两个可定位资源失败项，并从 HTTP API 与隔离数据库双重证明正式构建任务为 `0`。

只读复核入口为 [v2-branding-readonly.sh](./deploy/acceptance/v2-branding-readonly.sh)，机器证据写入 `docs/enterprise/evidence/v2-branding-readonly-20260815.json`。负向收口入口为 [v2-branding-isolated-e2e.sh](./deploy/acceptance/v2-branding-isolated-e2e.sh)，其调用 [v2-branding-negative-e2e.sh](./deploy/acceptance/v2-branding-negative-e2e.sh) 并生成 `docs/enterprise/evidence/v2-branding-negative-e2e-20260815.json`。证据记录共享开发数据库未写入、临时业务行/对象/额度预占均为 `0`，隔离容器、卷和网络残留均为 `0`，文件权限为 `0600`。

## 2. 现有合成构建证据

| 项目 | 证据 |
| --- | --- |
| 品牌配置 | `id=1`，AppName=`AppForge 白标验收`，API Host=`https://api.acceptance.example.com`，资源模式，revision `1` |
| 品牌资源 | Logo 对象 `14`，SHA-256 `79658712f1b322e5a68107a06780c09d5779e100737af2efa631e9941f3466b1`；启动图对象 `15`，SHA-256 `2797e3894eb71cce4210195c4964a902be520157c644b0cd1ea1ad8834dfcb5e` |
| 兼容预检 | `id=1`，状态兼容；`apktool_decode`、`application_label`、`launcher_icon`、`splash_resource`、`apktool_rebuild` 五项均通过 |
| 构建任务 | `id=4`，`SUCCESS`，Builder `builder-dev-01`，attempt `1` |
| 源 APK | 对象 `13`，包名 `com.appforge.acceptance`，versionCode `20260814`，SHA-256 `534efb9812f5aad5b6bc3aabffd978245e3b978faa5fb8eaf919ba9e041e2aa2` |
| 产物 APK | 对象 `16`，包名和版本保持不变，SHA-256 `f76f8448b7960bbfb5e7c818ada3886aaef82e9de8915ebb30ffd7653becefe1` |
| 签名 | APK Signature Scheme v1/v2/v3 均验证通过，单一签名者 |

产物 `assets/appforge/branding.json` 包含 profile `1`、revision `1`、配置的 AppName/API Host，以及 Logo/启动图原始对象 SHA-256；`channel.json` 的 task、版本、渠道和 API Host 与任务快照一致。解码后的 Launcher Logo 和启动图资源均相对源 APK 发生变化，应用 label 为配置值。

## 3. 严格验收矩阵

| # | 状态 | 当前证据 | 剩余工作 |
| --- | --- | --- | --- |
| 1 | 通过 | Core 对详情执行 `ensureTenant`，列表 SQL 固定 `tenant_id`；品牌配置、图片、预检、任务的数据库跨租户/跨应用引用只读核对为 0 | 持续保留跨租户回归 |
| 2 | 通过 | 新增真实 PNG/WebP 头、Logo 正方形及 512–2048 边界、启动图短边 720、伪造图片、动画 WebP、扩展名、5/10 MiB 和租户 Key 前缀单测 | 持续保留负向测试 |
| 3 | 通过 | 唯一临时 Compose 环境通过公共管理 API 创建合成应用、对象、版本、品牌配置和预检；真实 Worker `builder-dev-01` 解码并重建 APK 后返回 `INCOMPATIBLE`，报告明确 `launcher_icon` 与 `splash_resource` 目标缺失；HTTP API 和隔离数据库均确认正式构建任务为 `0`；临时数据、对象和净用量恢复后，容器/卷/网络残留均为 `0` | 持续保留隔离负向 E2E |
| 4 | 通过 | 任务 `4` 自动完成为 `SUCCESS`，产物对象 READY/BOUND 且摘要一致 | 持续保留真实 Builder 回归 |
| 5 | 通过 | `aapt` 读取源和产物包名均为 `com.appforge.acceptance` | 无 |
| 6 | 通过 | `aapt` 读取产物 application label 为 `AppForge 白标验收` | 无 |
| 7 | 通过 | 解码后的 Logo/启动图均相对源资源变化；`branding.json` 固化两个输入对象 SHA-256 | 持续保留像素/摘要门禁 |
| 8 | 通过 | `branding.json` 和 `channel.json` 的 API Host、profile、revision、task 均与任务快照一致 | 无 |
| 9 | 通过 | 所有品牌任务均保存 profile/revision/JSON，数据库只读检查不存在缺失或身份不一致快照；任务 `4` 的历史产物仍与其 revision `1` 快照一致 | 持续保留更新配置后的历史产物回归 |
| 10 | 通过 | 当前数据库存在 `builder_attempt>1` 后成功的品牌任务；V4 任务 `32/33` 已实测租约恢复、旧 attempt 回写拒绝和孤儿对象补偿 | 与 V4 fencing 门禁共同维护 |
| 11 | 通过 | `apksigner verify` 通过；V3 Android 10 ARM64 Emulator 的旧版安装、启动和覆盖升级沿用同一 V2 AppName、Logo、启动图和 API Host 品牌快照 | 后续授权设备回归继续复验 |
| 12 | 通过 | Core/API/Builder 定向测试、统一 Go 回归、前端类型检查/生产构建和 Docker 重建已有运行记录；无写入负向 fixture 已实际执行并接入统一回归 | 代码变更后持续重跑 |

## 4. 新增自动化门禁

- `appforge-api/internal/logic/storage_helper_test.go`：真实图片头、尺寸、静态/动画 WebP 和伪造文件。
- `services/core/internal/logic/brandinghelpers_test.go`：HTTPS/localhost API Host、凭据/路径/查询拒绝、资源目标、Runtime JSON、对象大小/扩展名/租户前缀、快照身份，以及隔离/Drain/过期心跳/并发满/低磁盘/能力缺失节点不得领取品牌预检。
- `services/builder/internal/worker/branding_test.go`：构建快照完整性、预检失败结构和资源目标定位。
- `deploy/acceptance/v2-branding-readonly.sh`：数据库引用不变量、历史任务/预检/对象、MinIO 字节、APK 静态结果和上述单测的统一只读复核。
- `deploy/acceptance/v2-branding-negative-e2e.sh`：保留默认拒绝写入和 fixture 模式；写入模式在创建应用前核对可调度 branding Builder，强制 API、Compose 项目、容器标签、网络和预签名对象 Origin 与调用方指定的隔离环境一致，随后验证 HTTP API → 真实 Builder → `INCOMPATIBLE` → 无正式任务 → 数据、对象和净用量清理。
- `deploy/acceptance/v2-branding-isolated-e2e.sh`：创建唯一临时 Compose 项目和随机端口，复用当前应用镜像启动全新 MySQL/Redis/etcd/MinIO/Core/System/Builder/API/单 Worker；执行负向 E2E 后 `down -v` 并验证容器、卷和网络均无残留。全新数据库运行同时发现并修复开发 Compose 迁移脚本按字典序导致 `100` 早于 `40` 的问题，现与生产迁移入口一致使用自然版本排序。

## 5. 收口运行记录

2026-08-15 执行 `deploy/acceptance/v2-branding-isolated-e2e.sh` 成功。隔离项目 `appforge-v2-isolated-1786796097-69179` 中，真实 Worker attempt `1` 完成预检；APK 解码、应用 label 和未修改重建通过，Launcher Logo 与启动图目标缺失，最终状态为 `INCOMPATIBLE`。正式构建任务为 `0`，临时业务行、对象字节、额度预占、容器、卷和网络残留均为 `0`。证据见 `docs/enterprise/evidence/v2-branding-negative-e2e-20260815.json`。
