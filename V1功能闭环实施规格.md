# AppForge V1 功能闭环与实施规格

## 1. 文档目的

本文档把《APK 渠道动态打包平台方案》中的产品目标转换为可以直接开发和验收的 V1 规格。

V1 完成的定义不是“页面、表或 RPC 已存在”，而是一个租户客户可以独立完成以下完整流程：

```text
登录代理端
  → 创建应用
  → 上传原始 APK
  → 创建应用版本
  → 上传签名文件并配置签名
  → 创建推广渠道
  → 创建渠道构建任务
  → Builder 注入 channelCode、重新签名并上传产物
  → 获得渠道下载链接
  → APK 首次启动上报
  → 后台查看渠道转化统计
```

任何一段只能手工改数据库、手工上传 MinIO、手工填写对象地址或依赖开发人员执行命令，都不视为 V1 完成。

## 2. 当前基线与缺口

### 2.1 已有能力

- `system` 服务已有租户、用户、角色、菜单、系统配置、登录日志和操作日志能力。
- `core` 服务已有应用、版本、签名配置、渠道、构建任务、安装归因和渠道事件的数据模型及基础 RPC。
- `builder` 服务已有领取任务、心跳、进度、成功和失败状态 RPC。
- `admin-api` 已提供系统管理和 Core 业务 HTTP API。
- `admin-ui` 已提供平台管理页面。
- Docker 开发环境已有 MySQL、Redis、etcd、MinIO、RPC、API 和管理前端。

### 2.2 V1 阻断性缺口

| 模块 | 当前状态 | V1 要求 |
| --- | --- | --- |
| 代理/客户入口 | 只有平台管理端 | 独立 `agent-ui`，只呈现当前租户业务能力 |
| 原始 APK 上传 | 只能手填 `sourceApkUrl` | 浏览器选择文件、上传、校验、计算 SHA-256 并关联版本 |
| 签名文件上传 | 只能手填对象 Key | 上传 JKS/PKCS12、私有保存、验证别名与密码 |
| 对象安全 | MinIO 开发桶允许匿名下载 | 私有桶、租户隔离、临时签名下载地址 |
| Builder 执行 | 只有任务状态 RPC | 常驻 Worker 完成下载、注入、对齐、签名、验证、上传 |
| 渠道注入协议 | 未定义 | 固定资产文件格式和 Android 读取协议 |
| 下载链接 | 只有数据库字段 | 可访问的渠道下载接口、下载事件和权限/有效期策略 |
| 构建日志 | 只有 `log_url` 字段 | Worker 采集并上传日志，页面可查看失败原因 |
| 端到端验收 | 无 | 使用真实 APK 完成上传到下载及归因的自动化验收 |

## 3. V1 产品边界

### 3.1 两个使用端

#### 平台管理端 `admin-ui`

面向 AppForge 平台运营人员，负责：

- 租户、租户域名、平台用户和权限管理。
- 系统配置、对象存储配置和审计日志。
- 跨租户问题排查和构建任务运维。
- 平台级统计和服务健康状态。

#### 代理端 `agent-ui`

面向租户 Owner、管理员和只读成员，负责：

- 当前租户的应用和版本。
- 原始 APK 上传。
- 当前租户的签名配置。
- 推广渠道、构建任务、构建日志和 APK 下载。
- 当前租户的渠道归因与转化统计。
- 当前租户成员及角色。

代理端不得接受前端传入的任意 `tenantId` 作为数据边界。租户必须来自已验证 JWT，由 API/RPC 元数据向下传递。

### 3.2 应用范围

系统账号应用范围固定为：

- `APPLICATION_SCOPE_ADMIN = 1`：平台管理端。
- `APPLICATION_SCOPE_AGENT = 2`：代理端。

现有的 `APPLICATION_SCOPE_LIQUIDITY = 2` 与本项目无关，实施时替换为 `APPLICATION_SCOPE_AGENT = 2`，数值保持不变以兼容已有数据。

### 3.3 V1 不包含

- Android 源码托管和从源码执行 Gradle 构建。
- iOS 打包。
- 白标资源替换、应用名称和图标替换。
- 套餐计费、在线支付和自动扩缩容。
- 第三方 KMS/Vault 的完整生产接入；接口保留，开发环境使用本地密钥加密。
- CDN 自动配置。

## 4. APK 渠道注入协议

原方案同时写了“客户提供 APK”和“Gradle Build”，两者不能直接组成同一条 V1 流程。V1 采用“修改已编译 APK 后重新签名”的方式，不要求客户上传 Android 源码。

Builder 在 APK 中写入固定资产文件：

```text
assets/appforge/channel.json
```

文件格式：

```json
{
  "schemaVersion": 1,
  "channelCode": "google_ads_001",
  "appId": 1001,
  "versionId": 2001,
  "buildTaskId": 3001
}
```

约束：

- `schemaVersion` 必须为整数，V1 固定为 `1`。
- `channelCode` 与构建任务快照一致。
- Android 客户端通过 `AssetManager` 读取该文件。
- 修改 APK 后原签名失效，因此必须移除旧签名、执行 `zipalign`、使用租户签名重新签名并执行 `apksigner verify`。
- Builder 必须验证输出 APK 的包名与应用配置一致。

后续如采用 APK Signing Block/Walle 等快速渠道方案，必须提升协议版本并保持 V1 读取兼容。

## 5. 文件与对象存储设计

### 5.1 对象类型

V1 定义以下固定对象类型：

| 枚举 | 中文说明 | 可见性 |
| --- | --- | --- |
| `SOURCE_APK` | 客户上传的原始 APK | 私有 |
| `KEYSTORE` | JKS/PKCS12 签名文件 | 私有且仅 Builder 可读取 |
| `BUILT_APK` | 渠道构建产物 | 私有，通过下载接口临时授权 |
| `BUILD_LOG` | Builder 构建日志 | 私有 |

### 5.2 对象 Key

对象 Key 由服务端生成，禁止使用客户原始文件名作为路径：

```text
tenants/{tenantId}/{objectType}/{yyyy}/{mm}/{uuid}.{ext}
```

数据库保存对象 Key，不保存永久公开 URL。对外下载时生成短时有效的签名 URL，或由 API 流式转发。

### 5.3 上传限制

- 原始 APK 最大 `2 GiB`，扩展名必须为 `.apk`。
- APK 必须是有效 ZIP，至少包含 `AndroidManifest.xml`。
- 上传过程流式计算 SHA-256，不允许把完整 APK 读入内存。
- 签名文件最大 `10 MiB`，仅允许 `.jks`、`.keystore`、`.p12`、`.pfx`。
- 服务端忽略浏览器提交的 Content-Type，依据扩展名和文件头二次判断。
- 上传失败必须删除未完成对象；孤儿对象由清理任务回收。

### 5.4 元数据表

Core 服务新增 `t_storage_object`，由 Core 独占维护：

- `id`、`tenant_id`、`app_id`。
- `object_type`、`object_key`、`original_name`、`content_type`。
- `size_bytes`、`sha256`。
- `status`：`UPLOADING`、`READY`、`BOUND`、`DELETED`、`FAILED`。
- `create_by`、`create_time`、`update_time`。

版本、签名配置和构建任务通过对象 ID 关联。原有 URL/Key 字段暂时保留用于兼容，但新流程以对象 ID 为权威来源。

## 6. HTTP 与 RPC 契约

### 6.1 代理端 HTTP API

代理端统一使用 `/agent` 前缀并校验 `APPLICATION_SCOPE_AGENT`：

```text
POST /agent/auth/login
GET  /agent/auth/profile

POST /agent/uploads/initiate
POST /agent/uploads/:id/complete
GET  /agent/storage/objects/:id/download

GET/POST /agent/core/applications
GET/POST /agent/core/versions
GET/POST /agent/core/signing-configs
GET/POST /agent/core/channels
GET/POST /agent/core/build-tasks
GET      /agent/core/build-tasks/:id/log
GET      /agent/core/channel-stats
GET      /agent/core/channels/:id/download
```

平台管理端可以保留 `/admin` 前缀。两组 HTTP 接口可由同一 API 进程承载，但路由、权限和审计规则必须分开。

### 6.2 上传协议

V1 使用对象存储预签名直传，避免最大 2 GiB 的 APK 经过 API 进程或被完整读入内存：

1. 前端调用 `POST /agent/uploads/initiate`，提交 `appId`、对象类型、文件名和文件大小。
2. API 创建 `UPLOADING` 元数据并返回短时有效的 PUT 地址。
3. 浏览器直接 PUT 到对象存储并显示上传进度。
4. 前端调用 `POST /agent/uploads/:id/complete`。
5. API 流式读取对象，验证类型、APK 结构、大小和 SHA-256，再把状态更新为 `READY`。

初始化响应：

```json
{
  "code": 200,
  "msg": "OK",
  "data": {
    "objectId": 10001,
    "uploadUrl": "https://storage.example/upload-signature",
    "expiresAt": 1710000000
  }
}
```

完成响应：

```json
{
  "code": 200,
  "msg": "OK",
  "data": {
    "objectId": 10001,
    "originalName": "release.apk",
    "sizeBytes": 12345678,
    "sha256": "...",
    "objectType": "SOURCE_APK"
  }
}
```

创建版本必须传 `sourceApkObjectId`，不得继续要求客户手填 `sourceApkUrl` 和 SHA-256。

创建签名配置必须传 `keystoreObjectId`、`keyAlias` 和受保护的密码字段。查询接口不得返回密码密文。

### 6.3 Core RPC

Core RPC 增加：

- 创建、完成和查询存储对象元数据。
- 绑定对象到版本或签名配置。
- Builder 按任务领取内部对象引用。
- 构建完成时绑定产物和日志对象。
- 为授权用户生成短时下载凭证。

跨服务不得直接查询其他服务负责的表。

## 7. Builder Worker

### 7.1 部署形态

`builder-rpc` 保留任务控制 RPC；新增独立常驻 `builder-worker` 容器。Worker 通过 Builder/Core RPC 领取任务，不直接访问 Core 数据库。

Worker 镜像至少包含：

- JRE/JDK。
- Android Build Tools 中的 `zipalign` 和 `apksigner`。
- 用于安全修改 ZIP 的工具或 Go 实现。

### 7.2 状态机

```text
PENDING
  → BUILDING（下载、校验、注入渠道文件）
  → SIGNING（zipalign、签名、签名校验）
  → UPLOADING（上传 APK 和日志）
  → SUCCESS

任意执行态 → FAILED
租约超时 → 可被其他 Worker 重领，builder_attempt + 1
```

每次状态变化必须校验 `builder_id` 和租约，避免过期 Worker 覆盖新任务结果。

### 7.3 工作目录和清理

- 每个任务使用独立临时目录。
- 文件名不得来自未经清洗的用户输入。
- 成功、失败或进程取消后都必须清理本地 APK、签名文件和密码临时文件。
- 日志不得输出签名密码、对象存储密钥或 JWT。

## 8. 渠道下载与归因

### 8.1 推广链接

渠道推广链接指向公开落地/下载路由，例如：

```text
GET /d/{channelCode}
```

处理流程：

1. 校验渠道启用且存在成功构建产物。
2. 写入幂等的下载/点击事件。
3. 返回短时签名下载地址或执行 302 跳转。

不得把 MinIO Access Key 暴露给浏览器，不得把整个桶设置为匿名公开。

### 8.2 客户端归因

- APK 从 `assets/appforge/channel.json` 读取 `channelCode`。
- 首次启动生成稳定 `installId` 并调用 `/api/install/report`。
- `installId` 首次绑定渠道后不可被后续请求覆盖。
- 注册、首充、支付通过幂等 `eventKey` 上报 `/api/channel/event`。

## 9. 代理端页面

V1 `agent-ui` 页面：

```text
工作台
应用管理
├── 应用列表
└── 版本管理（上传原始 APK）
签名管理（上传签名文件）
渠道管理
├── 渠道列表
├── 创建构建
└── 推广链接
构建中心
├── 构建任务
├── 构建日志
└── APK 下载
渠道统计
团队
├── 成员
└── 角色
```

所有列表页遵循统一布局：无重复标题卡、表格填满剩余空间、分页固定在右下角。

## 10. 安全要求

- JWT 必须包含并校验 `tenantId`、`userType`、`appScope`。
- Agent API 禁止跨租户查询和对象下载。
- 上传和下载均写操作审计或下载事件。
- 签名密码只允许加密传输和加密保存；日志、响应和构建日志中不得出现。
- Keystore 和原始 APK 使用私有对象权限。
- 公共安装/事件接口必须有限流、参数校验和幂等约束。
- 构建命令不得拼接未经校验的 shell 参数，使用参数数组启动子进程。

## 11. Docker 开发部署

V1 Compose 至少包括：

```text
mysql
redis
etcd
minio
system-rpc
core-rpc
builder-rpc
builder-worker
admin-api
admin-ui
agent-ui
```

开发 MinIO 初始化必须创建私有桶。端到端验收使用真实对象存储，不使用手工预置 URL。

## 12. 实施顺序

### 阶段 A：文件链路

- 增加对象元数据表、枚举和 RPC。
- 改造对象存储为流式上传和私有下载。
- 增加 APK/Keystore 上传 API。
- 版本和签名配置改为绑定对象 ID。
- 在现有管理端先接入上传控件，验证后复用到代理端。

### 阶段 B：Builder 闭环

- 增加 Worker 进程和镜像。
- 实现任务领取、心跳和租约恢复。
- 实现 APK 校验、渠道文件注入、zipalign、签名与验证。
- 上传产物/日志并完成任务。

### 阶段 C：代理端

- 将应用范围 2 调整为代理端。
- 创建代理端登录、菜单、业务路由和权限种子。
- 创建 `agent-ui` 并接入上传、构建和下载流程。
- 加入 `agent-ui` Docker 服务。

### 阶段 D：分发归因

- 实现推广下载路由和短时签名地址。
- 完善点击、下载、首次启动、注册、首充和支付事件。
- 完善渠道统计和时间范围查询。

## 13. V1 验收标准

必须同时满足：

1. 新建租户后，租户 Owner 可以登录代理端且无法看到其他租户数据。
2. 用户可以从浏览器上传一个真实 APK；服务记录正确大小和 SHA-256。
3. 用户可以上传 Keystore，错误密码或错误别名不能成功进入可用状态。
4. 用户创建渠道构建任务后，无需人工操作，任务最终进入 `SUCCESS`。
5. 产物 APK 中存在正确的 `assets/appforge/channel.json`。
6. `apksigner verify` 通过，包名与应用配置一致。
7. 用户可以通过代理端下载渠道 APK，匿名用户不能枚举私有对象。
8. APK 首次启动上报后，渠道统计出现对应安装；重复上报不重复计数。
9. Builder 中断后租约可恢复，过期 Worker 不能覆盖新 Worker 的结果。
10. `go test ./...`、前端类型检查和生产构建通过。
11. Docker 全量重建后所有长期服务健康，端到端验收从空业务数据开始可重复执行。

只有以上验收全部有实际运行证据，才能声明 AppForge V1 完成。

## 14. 实施结果与验收证据（2026-08-13）

本节记录当前实现的实际运行结果，不以接口或页面存在代替业务验收。

1. 已通过事务创建验收租户及 Owner。租户 1 只能查询到应用 `v1_acceptance`，租户 2 只能查询到 `tenant2-isolation-app`；匿名访问代理端应用列表返回 `401`。
2. 已通过浏览器同源直传链路上传真实 APK。原始对象 ID 为 5，记录大小 530 字节，SHA-256 为 `173eabe909d6f47ac60ece6b36a51d22b5a034e9285b6147afaeb9f3aa85e950`；代理端上传页面显示实时进度。
3. 已上传验收 Keystore；正确口令和别名可用，错误口令在创建签名配置时返回 `400`，使用相同密文材料校验不存在的别名返回 `InvalidArgument`，均不会进入可用状态。签名口令使用 AES-GCM 密文保存，审计日志递归脱敏。
4. 构建任务 ID 3 由 Worker 自动领取并进入 `SUCCESS`，产物对象 ID 为 11，构建日志对象 ID 为 12，无人工修改任务状态。
5. 产物包含 `assets/appforge/channel.json`，其中 `tenantId=1`、`appId=1`、`versionId=2`、`buildTaskId=3`、`channelCode=v1e2e`、`versionCode=101`。
6. 产物大小为 8570 字节，SHA-256 为 `ac69a2d496dc02c6c2ca46e3ab735f303d49c2c3986ae5af2340e5a7987c622e`；`apksigner verify` 的 v1、v2、v3 均通过，`aapt` 读取包名为 `com.appforge.acceptance`。
7. 公开链接 `/d/v1e2e` 返回短时签名地址；MinIO 原始对象匿名访问返回 `403`，代理端私有业务 API 匿名访问返回 `401`。
8. 首次启动和注册使用唯一事件键及数据库唯一约束实现幂等。重复上报后，安装 ID `v1-e2e-install-20260813` 在安装表中仍只有 1 条，首次启动、注册事件各只有 1 条。
9. 已实际制造 Worker 租约过期并由新 Worker 恢复任务；旧 `builder_attempt` 提交结果被拒绝，新 attempt 可继续并最终完成，过期 Worker 不能覆盖结果。
10. `common`、`appforge-api`、`services/system`、`services/core`、`services/builder` 和四个 Proto 模块的 `go test ./...` 全部通过；前端 `npm run type-check` 与 `npm run build` 通过。
11. `docker compose -f deploy/docker-compose.dev.yml up -d --build` 全量执行成功；长期服务均处于运行或健康状态，初始化和迁移容器正常以状态码 0 退出，近期日志无 panic、fatal 或 error。管理端 `5173`、代理端 `5174` 均返回 `200`。

补充安全验证：System、Core、Builder RPC 已启用内部令牌拦截，未携带内部令牌直接调用 Builder RPC 返回 `Unauthenticated`；3 个历史遗留上传对象已由 Worker 清理并标记为已删除。开发环境内部令牌和默认账号仅用于本地 Compose，生产部署必须替换。
