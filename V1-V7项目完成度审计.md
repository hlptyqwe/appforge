# AppForge V1–V7 项目完成度审计

## 1. 审计结论

审计日期：2026-08-17。

当前项目不能判定为“V1–V7 全部完整”。功能实现已经覆盖总体方案的主干，但严格状态为：

| 阶段 | 最新状态 | 严格结论 |
| --- | --- | --- |
| V1 | 完成 | 11 项渠道上传、构建、签名、分发和归因标准有运行证据 |
| V2 | 完成 | 12 项全部通过；不兼容 APK 的公共 API → 真实 Worker 负向 E2E 已在唯一临时 Compose 环境通过 |
| V3 | 完成 | 10 项全部通过且有 Android Emulator 安装/升级历史证据 |
| V4 | 完成 | 12 项有历史运行证据；清理约 2.435GB 可重建缓存并经真实管理 API 恢复三节点后，最新当前验收再次全项通过 |
| V5 | 完成 | 11 项有历史运行证据，2026-08-15 当前只读不变量复核通过 |
| V6 | 完成 | 11 项有历史运行证据，2026-08-15 当前只读不变量与 Stripe 验签复核通过 |
| V7 | 实施中 | 13 项中 6 项通过、7 项部分通过；本地三种 Artifact 合成闭环已完成，客户/生产交付仍有缺口 |

因此，应把项目描述为“核心产品主干和本地企业能力已大范围落地，尚未达到 V1–V7 全量发布/客户交付完成”，不能描述为“项目已经完整”。

## 2. 当前权威证据

- V1：`V1功能闭环实施规格.md` 第 14 节的真实 APK、租户隔离、Builder 恢复、签名、下载和归因记录。
- V2：`V2基础白标验收报告.md`、`docs/enterprise/evidence/v2-branding-readonly-20260815.json` 与 `docs/enterprise/evidence/v2-branding-negative-e2e-20260815.json`；后者记录公共 API、真实 Worker、`INCOMPATIBLE`、零正式任务、共享开发数据库未写入和隔离栈零残留。
- V3：`V3高级白标验收报告.md`、历史 Android 10 Emulator 运行记录与 `docs/enterprise/evidence/v3-whitelabel-readonly-20260815.json`。
- V4：`V4构建集群验收报告.md` 的历史 12 项记录；最新失败状态以 `docs/enterprise/evidence/v4-build-cluster-current-20260815.json` 为准。
- V5：`V5开放平台验收报告.md` 与 `docs/enterprise/evidence/v5-open-platform-readonly-20260815.json`。
- V6：`V6商业化验收报告.md` 与 `docs/enterprise/evidence/v6-commercialization-readonly-20260815.json`。
- V7：`V7企业交付验收报告.md` 13 项严格矩阵及 `docs/enterprise/evidence/` 下 AIR_GAPPED、容量、网络策略、受限出口代理、Prometheus/OTLP、离线安装/升级、远程签名、Schema 113 灾备和 1.2.x Kubernetes 升级/回滚证据；正式 Kubernetes 证据为 `v7-formal-kubernetes-upgrade-v1.2.5-v1.2.6-20260817.json`。

“已有代码、字段、菜单、接口或脚本”不单独构成完成证据；当前失败证据优先于更早的通过记录。

## 3. 与总体方案的产品覆盖

总体方案要求的渠道、APK 版本、签名、Builder、下载、安装归因、注册/付费事件、渠道统计、基础/高级白标、构建集群、开放 API、Webhook/CLI、商业化、私有部署和 Local Agent 均已有对应实现和阶段文档。

开放平台不需要另建一套前端或 API 工程：开发者中心继续位于 `appforge-ui`，`/open/v1` 与管理/代理 HTTP 边界继续位于 `appforge-api`；CLI、Webhook Worker、源码触发 Worker 和 Local Agent 按运行职责独立。客户源 APK 的入口按部署模式分别为现有 Web 上传、Local Agent 客户存储导入或 AIR_GAPPED 签名任务包，不依赖新增“代理上传前端”才能工作。

生产版本边界已提升为 1.2.x/Schema 113。AIR_GAPPED 已具备平台/租户细粒度 RBAC、租户门户路由和权限化管理界面，可完成任务包导出/下载、状态查询、结果 ZIP 私有上传与签名导入；生产 Compose、Helm、迁移镜像、API 目标和 Release Workflow 已同步，空库安装、112→113 升级、幂等、已有角色零自动授权及本地断网全新安装均通过。`v1.2.6` 正式 tag/OIDC 发布、公开 Release 匿名下载和双层签名校验已通过；由真实签名发布资产生成的 18 镜像 `linux/amd64` 正式离线介质也已完成本地断网全新安装。两个不同 commit、16/16 个 AppForge 平台摘要不同的 `v1.2.5 → v1.2.6 → v1.2.5` 正式介质升级/回滚已在本地禁拉取环境闭环。原生 `linux/amd64` 正式 Kubernetes 门禁还完成双版本聚合 Sigstore 与 21 个镜像实时 Cosign 验证，9/9 个应用镜像摘要不同，升级/回滚连续探测分别成功 205/366 次且失败 0 次；客户物理断网、介质交接、现场升级以及目标 Kubernetes/CSI/Ingress/私有仓库仍是独立外部门禁。

## 4. 可在本地继续收口的事项

1. V7：保持 1.2.x/Schema 113 生产边界，继续补齐不依赖客户环境的代码测试、文档、发布门禁和可重复机器证据。

## 5. 需要外部环境或新增授权的事项

- 客户测试 S3、阿里云 OSS、真实 AWS 工作负载身份和最小权限。正式 Local Agent 已提供只触碰登记前缀单个随机合成对象的现场探针，临时 MinIO 夹具及公开原生 `linux/amd64` CI 已验证完整回读、删除确认、既有对象不变、目标/凭据脱敏及零残留；`v1.2.7` 正式签名 digest 还通过版本启动核验。仍须在客户测试目标运行并归档 `environmentKind=customer-test` 证据，现有证据见 `docs/enterprise/evidence/v7-customer-storage-probe-release-v1.2.7-20260817.json`。
- 客户 Linux/物理断网介质、正式证书、介质交接和现场安装/升级；具有真实版本差异的签名镜像已完成本地禁拉取升级/回滚，不替代客户现场记录。
- Schema 113 已完成生产边界提升，`v1.2.6` annotated tag、远端 OIDC 正式发布、匿名 Release 资产验证、正式介质本地断网全新安装及 `v1.2.5 → v1.2.6 → v1.2.5` 正式版本差异升级/回滚已闭环；仍缺客户现场 AIR_GAPPED 专用角色授权复验。
- 远程 APK 签名模式持久化和 Core/API/Builder 任务分支已由本地合成 E2E 闭环；仍缺客户真实 HSM 的不可导出密钥、PIN/会话、HA、审计、限流和性能验收。
- 历史 Schema 112 兼容恢复与当前 Schema 113 代表性合成业务灾备均已完成；后者覆盖 AIR_GAPPED 状态、六个对象字节和 etcd，RPO 2 秒、RTO 11 秒。合成 MySQL UTC PITR，以及同机独立 MinIO 源/副本卷的版本化复制与源卷销毁恢复也已生成 0600 证据；生产规模业务数据隔离备份恢复、真实跨区域副本恢复及客户 RPO/RTO 仍待完成。
- CONNECT-only 出口代理及部署门禁已有本地合成 TLS 证据；SIEM 已补齐 HTTPS Webhook 和 RFC5424/RFC6587 Syslog TLS，真实回环 TLS/CONNECT、重连、危险协议/URL 拒绝和生产配置门禁通过，公开原生 `linux/amd64` Syslog、客户存储及正式 Kubernetes 回归也全部成功，证据见 `docs/enterprise/evidence/v7-syslog-tls-20260817.json` 与 `docs/enterprise/evidence/v7-syslog-tls-public-ci-20260817.json`。正式发布证据供应链已补齐 16 个 AppForge 与两个原始第三方运行镜像的 SPDX、许可证清单、Trivy、AppForge 逐镜像 Cosign 回验、源码依赖不导出明细的 High/Critical 阻断、18 镜像聚合清单/签名以及离线介质强制携带契约；`v1.2.6` 的远端 tag/OIDC/漏洞库、公开 Release 匿名下载、双层签名实测和正式介质本地断网全新安装均已闭环。真实双版本签名镜像的 Kubernetes 升级/回滚也已在原生 amd64 kind 闭环，但客户目标 CNI/CSI/Ingress/私有仓库、最终域名/IP allowlist、DNS/防火墙/代理容量和 Prometheus/OTLP/SIEM 解析/路由/留存/告警仍待闭环。
- 客户容量峰值、生产规模 APK/对象吞吐、天级长稳和 Android 授权真机复验；公开原生 `linux/amd64` 当前源码 Compose 已完成认证 API、真实合成 APK 构建和临时 MinIO 对象往返的并行小时级长稳，错误均为 0、8 个核心容器零重启，但不替代这些客户/生产门禁。

这些事项不能由本地合成测试替代，也不能通过放宽验收口径标记完成。

## 6. 下一次“项目完整”判定门禁

只有同时满足以下条件，才可把总目标标记为完成：

1. V7 的 13 项严格矩阵全部从“部分通过”转为“通过”。
2. V1–V7 全量回归使用当前源码、当前镜像和当前 Schema 边界重新执行成功。
3. 对应机器证据、客户/生产环境记录、文档、迁移、权限和发布材料同步更新。

在此之前，路线目标保持进行中。
