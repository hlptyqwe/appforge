# AppForge V7 企业交付验收报告

## 1. 结论

验收日期：2026-08-17。

V7 当前为“实施中”，不能标记完成。13 条严格验收中，6 条已通过，7 条部分通过，0 条完全未验收。当前 V1–V6 均已完成；三种 Artifact 模式均已完成合成 APK/Keystore 的真实构建闭环。AIR_GAPPED 最小 RBAC、1.2.x/Schema 113 生产提升、`v1.2.6` 正式 tag 安全发布、公开 Release 匿名下载、双层签名校验、正式介质本地断网全新安装、Compose 正式版本差异升级/回滚，以及原生 `linux/amd64` 上正式签名 `v1.2.5 → v1.2.6 → v1.2.5` Kubernetes 升级/回滚均已闭环，但真实客户对象存储、物理断网介质交接/现场升级、客户 Kubernetes 基础设施及其他外部环境交付证据仍有缺口。

状态定义：

- `通过`：该条全部要求已有实际运行证据。
- `部分通过`：已有实现和局部运行证据，但端到端或目标环境证据不完整。
- `未验收`：尚未执行该条要求的完整实演；已有脚本或配置不能替代验收。

## 2. 严格验收矩阵

| # | 状态 | 已有实现与证据 | 未完成项 |
| --- | --- | --- | --- |
| 1 | 通过 | Compose、Helm、values Schema、生产 preflight、迁移 Job 均已实现；已用当前源码构建固定版本镜像，分别在独立空数据卷 Private 与 Dedicated Compose 环境一次完成迁移、固定非 root UID、TLS 双前端、对应模式离线许可证状态、启动管理员登录和私有对象存储 Bucket 初始化；全程使用临时随机密钥且无开发默认凭据 | 持续在正式发布镜像与客户 Linux 环境保留安装回归 |
| 2 | 通过 | 同摘要 `1.2.0 → 1.2.1 → 1.2.0` kind v1.32.2 快速回归先完成 350 次集群内探测零失败。随后在原生 `linux/amd64` GitHub Runner 上验证 `v1.2.5` 与 `v1.2.6` 聚合 Sigstore 身份并实时 Cosign 回验 21 个镜像，确认 9/9 个 Helm 应用镜像摘要不同；一次性 kind v1.32.2 集群完成 `1.2.5 → 1.2.6 → 1.2.5` RollingUpdate 与应用回滚，升级 205 次、回滚 366 次 API 探测均成功且失败 0 次；回滚后 Schema 113、AIR_GAPPED 表及两个 API 副本保持。正式证据见 `docs/enterprise/evidence/v7-formal-kubernetes-upgrade-v1.2.5-v1.2.6-20260817.json`，快速证据见 `v7-kubernetes-upgrade-20260816.json` | 持续在客户目标 Kubernetes/CSI/Ingress/私有仓库环境保留同等回归；现有 RWX 许可证 Claim 必须预置 UID/GID 65532 写权限 |
| 3 | 部分通过 | 已在独立临时 Compose 环境完成 MySQL、etcd、对象数据的一致性备份，删除数据后恢复并逐项核对；基础演练得到 RPO 2 秒、RTO 9 秒；历史 Schema 112 兼容恢复严格排除 Schema 113，得到 RPO 2 秒、RTO 10 秒。当前 `v7-appforge-schema113-dr.sh` 使用 1.2.x/Schema 113 和代表性合成租户/应用/版本/渠道/签名/任务/审计/AIR_GAPPED 引用链完成完整破坏恢复，逐字节核对六个对象及 etcd，得到 RPO 2 秒、RTO 11 秒、58 张 AppForge 表。生产脚本还记录同点 binlog 坐标、归档关闭日志并用随离线包交付的同版本工具执行 UTC PITR；隔离 E2E 删除测试表后恢复出截止前事件并排除截止后事件，RTO 7 秒且资源零残留。MinIO 同机独立双卷 E2E 已验证双版本/删除标记复制、源卷销毁和副本恢复，复制/恢复均为 1 秒。0600 证据见 `docs/enterprise/evidence/v7-appforge-schema113-dr-20260816.json`、`v7-mysql-pitr-20260815.json` 与 `v7-object-replication-20260815.json` | 仍未使用客户目标基础设施或生产规模数据；真实跨区域故障域副本恢复和客户 RPO/RTO 仍需目标环境验收，复制现有开发业务数据前需要明确数据导出授权 |
| 4 | 通过 | Local Agent 使用主动出站 HTTP/mTLS 完成注册、心跳、领取、输入下载、执行、产物上传和回报；协议3白名单 Task Bundle、客户侧安全 Compose/安装升级包、本地 Secret 和固定 `appforge-local-build` 已实现。1.2.x 的协议2–3窗口现由控制面、Agent、AIR_GAPPED 和部署配置共享：协议1/4需升级且不能领取，协议2在线但不能领取协议3 Task Bundle，协议3正常领取；真实 MySQL 协议1–4验收见 `docs/enterprise/evidence/v7-agent-protocol-window-20260817.json`。`v7-control-plane-storage.sh` 与 `v7-customer-storage.sh` 分别完成控制面存储和受限临时客户 MinIO 的真实任务 E2E，最终 APK 均通过签名和包名校验 | 持续在客户网络、正式证书和正式发布镜像保留同等回归，并补充代表性旧版正式 Agent 二进制升级路径复验 |
| 5 | 通过 | 一次性注册码只存 SHA-256，管理端安装命令通过隐藏输入和 `--token-stdin` 传递注册码；Agent 私钥只在本地。真实 MySQL 验收已覆盖注册、证书签发、轮换后旧证书拒绝和管理员吊销后新证书拒绝；隔离 mTLS Gateway 下的真实 Agent 已检测临近到期证书、生成新私钥并自动轮换，原子切换本地证书后继续通信，Gateway 吊销开关随即拒绝当前轮换证书并记录证书序列号；管理页面显示已过期和 24 小时内到期告警 | 持续在正式环境保留长周期轮换、吊销和告警回归，并按客户要求接入外部告警通道 |
| 6 | 通过 | 协议只暴露固定任务操作，不提供 shell/任意命令；领取 SQL 强制 tenant、pool 和 allowed app；跨租户数据完整性检查与真实 MySQL 测试通过 | 持续保留跨租户与任意命令回归门禁 |
| 7 | 部分通过 | `CONTROL_PLANE_STORAGE` 已完成证书绑定单次 GET/PUT 票据、控制面二次开流校验和 Core 绑定。`CUSTOMER_STORAGE` 已完成显式对象归属、受限 `local-file://` S3/MinIO/OSS Secret、mTLS 输入登记和逐字节 SHA 校验；正式 Local Agent 还新增受限现场探针，只在登记前缀写入一个随机合成对象并执行 Put、Stat、完整回读 SHA-256、Delete 和权威 NotFound 确认，不 List/读取既有对象或修改桶/策略。临时 MinIO Provider E2E 已证明合成对象零残留、预置既有对象不变和报告不泄漏 endpoint/bucket/prefix/凭据，证据见 `docs/enterprise/evidence/v7-customer-storage-probe-fixture-20260817.json`。`AIR_GAPPED` 已完成 attempt/槽位/额度锁定、双向签名 ZIP、固定执行器、本地防重放及事务导入；租户门户路由与平台/租户各三条正式权限已补齐，Local Agent 管理页可按权限执行任务锁定导出、短时下载、状态查询、结果 ZIP 私有上传和签名导入。合成 E2E 先证明未授权 403、再显式授权专用角色并以 `--network none` 完成真实 APK；Schema 113 已作为 1.2.x 生产目标通过空库安装、112→113、重复迁移、数据保持、已有角色零自动授权及本地断网全新安装 | 现场工具已就绪，但真实 S3 与阿里云 OSS 仍需在客户测试前缀运行并归档 `environmentKind=customer-test` 证据；AIR_GAPPED 仍需客户物理介质及正式证书/镜像复验，因此三模式总项仍为部分通过 |
| 8 | 通过 | 真实 MySQL 故障注入已验证租约恢复、attempt fencing 和 Drain；隔离 mTLS Gateway 下，真实 APK 执行器在 attempt 1 运行中被 `SIGKILL`，新容器复用私有状态卷并以 attempt 2 完成签名 APK，防重放时间戳跨进程持续增长，旧 attempt 完成请求返回 409；`CONTROL_PLANE_STORAGE` E2E 还验证了被篡改 attempt 1 失败后 attempt 2 使用新票据恢复并完成；已导入固定镜像的离线升级、状态卷保留、不健康镜像自动回滚和旧镜像健康复核均已实测通过 | 持续在正式镜像和客户网络保留长任务、网络闪断与票据刷新回归 |
| 9 | 部分通过 | Builder Worker 已实际接入 local-file、k8s-secret、Vault、AWS Secrets Manager；Local Agent 已实现受限 `local-file://` 签名 Secret 和客户对象存储 Secret，并提供不暴露目标原值/凭据的 S3/MinIO/OSS 合成现场探针，临时 MinIO 的真实 SDK 路径、完整回读、删除确认和零残留已通过。`REMOTE_APK_SIGNER` 已具备固定 mTLS 协议、显式 API/RPC 枚举、Schema 112 兼容持久化、创建时身份校验、Core 执行快照、Worker 远程签名分支、最终 APK 证书校验和 `remoteSigning` 节点调度门禁。协议 E2E 已验证防篡改、错误身份、超时和跨重启防重放；任务 E2E 又验证公共管理 API、数据库零 Keystore/零密码、无能力节点不能领取、有能力真实 Worker 成功构建、最终包名/证书/渠道资产、Secret 日志扫描及隔离资源零残留。0600 证据见 `docs/enterprise/evidence/v7-remote-apk-signing-20260815.json` 与 `docs/enterprise/evidence/v7-remote-signing-task-20260815.json` | AWS、真实 S3 和阿里云 OSS 尚缺客户测试账户/工作负载身份真实验收；现场探针不替代 AWS 工作负载身份；远程签名仍需客户测试 HSM 验证不可导出密钥、PIN/会话、HA、审计、限流和性能 |
| 10 | 部分通过 | 离线镜像导入脚本和离线签名许可证已实现；许可证生成、部署绑定、持久防回拨、OCI 导入、Local Agent 安装包、CLI 和企业文档落盘校验通过。本地模拟断网已在独立空卷完成全新 Private 安装：全部镜像预载且 Compose `--pull never`，前后端双 internal 网络，内部 TLS 双前端/API/管理员登录可达而外部 HTTPS 被拒绝，迁移、固定非 root、离线许可证和私有对象存储初始化通过；Schema 门禁确认 112、113 迁移及 AIR_GAPPED 表均存在，生产目标为 113，证据见 `docs/enterprise/evidence/v7-offline-install-20260815.json`。同一环境还完成预载应用镜像 `1.2.0 → 1.2.1 → 1.2.0` 离线升级/回滚，数据库与对象标记全程保持。离线包现在必须先以工作流身份验证聚合 Sigstore 证据，按签名清单中的 digest 拉取 16 个 AppForge 发布镜像和两个原始第三方运行镜像，并把 SPDX、许可证、Trivy、逐镜像 Cosign 回验及聚合清单完整落盘。`v1.2.6` 四项公开 Release 资产已匿名下载并通过外层/内层 SHA、Sigstore 身份与 CLI 权限校验；随后由真实签名资产生成 3,324,800,000 字节正式介质，SHA-256 为 `a88b792badcf39bd1522fd1e1923eb8226c0fa8f51e5ebb5cc5e9ecf9f46c306`，包含 18 个固定为 `linux/amd64` 的镜像及 18 条签名索引到平台摘要映射，并通过 SHA 校验、镜像加载、自包含 Admin API 模板、独立配置渲染和 `--pull never` 本地断网全新安装，证据见 `docs/enterprise/evidence/v7-public-release-v1.2.6-20260816.json` 与 `docs/enterprise/evidence/v7-formal-offline-media-v1.2.6-20260816.json`。进一步使用 `v1.2.5` 与 `v1.2.6` 两个不同 Git commit 的正式签名介质完成 `1.2.5 → 1.2.6 → 1.2.5`：16/16 个 AppForge 平台摘要不同，发布部署文件、目标迁移镜像、应用服务及 MySQL/etcd/MinIO 实际切换后回滚，数据库与对象标记保持；261 次探针失败 6 次、最大连续失败 3/门禁 10，证据见 `docs/enterprise/evidence/v7-formal-offline-upgrade-v1.2.5-v1.2.6-20260817.json` | 本地 Docker internal 网络不等于客户物理断网；尚未完成客户正式介质交接，也未在客户物理断网环境使用正式证书/硬件复验安装、升级和回滚并归档记录 |
| 11 | 部分通过 | 发布工作流对 16 个 AppForge 发布镜像和两个原始第三方运行镜像按 digest 生成 SPDX、独立许可证清单并执行 Trivy High/Critical 门禁；16 个发布镜像由 Cosign keyless 签名并保存逐镜像回验 JSON。MySQL、etcd、MinIO 和 mc 固定上游源码提交并升级已知漏洞依赖后构建，保留上游许可证和来源标签。Go module 与前端 manifest/lockfile 还有独立 Trivy 文件系统 High/Critical 阻断；依赖明细不上传 Artifact/SARIF，聚合 Manifest 只记录成功状态和 `detailsExported=false`。npm 官方生产及全部依赖审计均为零漏洞，Vite 8.2.1 与 Vue 插件 6.0.8 的类型检查、生产构建和双前端 Docker 重建通过，0600 证据见 `docs/enterprise/evidence/v7-npm-audit-20260816.json`。全部 18 个镜像再汇总版本/Git commit/digest 清单和 SHA256SUMS，由同一工作流 OIDC 身份签名并回验，Redis、Alpine 来源由聚合签名锁定而不冒充自签。合成契约验收已验证源码门禁失败阻止汇总、固定文件集合、跨版本/仓库约束和 SHA 篡改拒绝；`v1.2.6` 真实 tag 流水线以提交 `3d1fde7` 完成 22/22 任务，真实 OIDC/Cosign/Trivy、聚合签名、匿名 GHCR Manifest、公开 Release 四资产、匿名双层签名校验及正式离线介质本地安装均通过。Helm/Calico 网络策略、CONNECT-only 代理和 Prometheus/OTLP/JSON 日志均有本地运行证据。SIEM 现同时支持 HTTPS JSON Webhook 与 RFC5424/RFC6587 Syslog TLS；真实回环 TLS 已验证自定义 CA、两次连接、结构化字段转义、64 KiB 门禁、明文/危险 URL 拒绝及受限 HTTP CONNECT，公开原生 `linux/amd64` 门禁及关联客户存储/Kubernetes 回归全部成功，证据见 `docs/enterprise/evidence/v7-syslog-tls-20260817.json` 与 `docs/enterprise/evidence/v7-syslog-tls-public-ci-20260817.json` | 仍缺客户目标 CNI/Ingress/最终 allowlist、DNS/防火墙/代理容量及客户 Prometheus/OTLP/SIEM 解析、路由、留存和告警联调记录；远程签名与 Webhook 受控直连仍需目标环境放行；兼容字段 `egressCIDRs` 新交付禁止使用 |
| 12 | 部分通过 | 隔离合成 MySQL、etcd、对象数据已完成基础破坏恢复计时；当前 Schema 113 代表性业务链进一步完成完整 Docker 破坏恢复，RPO 2 秒/目标 900 秒、RTO 11 秒/目标 7200 秒，58 张 AppForge 表、AIR_GAPPED 状态、业务摘要、六对象清单摘要和 etcd 配置均核对通过；历史 Schema 112 兼容恢复证据继续保留。合成 MySQL PITR 已验证基线坐标、日志归档校验、UTC 截止时间回放和截止后事件排除，RTO 7 秒；同机独立 MinIO 源/副本卷已完成双版本、删除标记、源卷销毁和副本恢复。Compose 正式介质以两个不同 commit、16/16 个不同平台摘要完成 `v1.2.5 → v1.2.6 → v1.2.5` 禁拉取升级/回滚，数据库与对象标记保持；Kubernetes 正式签名双版本也完成同一升级/回滚，9/9 个应用镜像摘要不同，升级/回滚探测分别成功 205/366 次且失败 0 次，回滚后 Schema 113、AIR_GAPPED 表与双 API 副本保持 | 仍需生产规模业务摘要、真实跨区域恢复、客户 RPO/RTO 计时，以及客户目标环境中的正式介质和 Kubernetes 升级回滚演练记录 |
| 13 | 部分通过 | `deploy/acceptance/v1-v7-regression-base.sh` 已用当前源码、当前 Core 镜像和当前 Schema 边界重新通过 11 个 Go 模块全量测试/编译、Node v20.20.2 前端类型检查、全部交付 Shell 语法、V2 无写 fixture/只读复核、V3 只读复核、V4 当前三节点、V5、V6、V7 基础运行验收，并在本次运行中显式启用隔离远程签名完整任务 E2E；V7 运行链同时执行 18 镜像发布证据合成契约和离线打包落盘。性能冒烟以 32 并发完成 500 次 healthz、12 并发完成 150 次带 JWT/RBAC/RPC/数据库读取的部署状态请求，均 0 错误。AIR_GAPPED 最小 RBAC 与 Schema 113 生产迁移另有独立 E2E，合成本地容量验收还覆盖 4 个真实 APK 并发构建、临时 MinIO 每方向 128 MiB 和 8 并发 120 秒 API 稳定性。公开原生 `linux/amd64` 当前源码 Compose 先完成 3600 秒认证 API 长稳，随后在同等当前源码栈上并行运行认证 API、Local Agent 真实合成 APK 打包/签名和 1 MiB 临时 MinIO 对象完整往返；API/构建/对象错误均为 0，至少完成 120 次 APK 构建和 600 次 Put/Stat/Get/SHA/Delete/NotFound（每方向至少 600 MiB），MySQL/Redis/etcd/MinIO/System/Core/Builder/API 8 个容器零重启，证据见 `docs/enterprise/evidence/v7-api-soak-hour-public-ci-20260817.json` 与 `docs/enterprise/evidence/v7-artifact-mixed-soak-hour-public-ci-20260817.json`。npm 官方生产和全部依赖审计为零漏洞；`v1.2.6` 真实 tag/OIDC/漏洞库、公开资产匿名下载、正式离线介质本地断网全新安装、Compose 正式版本差异升级/回滚，以及原生 amd64 正式 Kubernetes 双版本升级/回滚也已通过 | 尚未执行 Android 授权真机、客户目标环境容量/峰值、生产规模 APK/对象存储吞吐、天级长稳和剩余外部集成；当前小时级证据使用合成 APK、测试 Keystore、临时 MinIO 和单公开 Runner，不代表客户生产峰值或容量承诺。真实 AWS/S3/OSS、客户物理断网、客户 Kubernetes 和客户 SIEM 等缺口未闭环前不能标记全量通过 |

第 13 条补充证据：2026-08-17 已在无快照只读 Android 10（API 29）ARM64 AVD 上，使用当前脚本提交 `2a06f471851583a67cc7c86a5c9f37e7b1a18550` 和既有合成任务 14→15，重新通过旧版安装/启动/UI、同签名原位升级、新版启动/UI 与 `firstInstallTime` 保持。0600 机器证据见 `docs/enterprise/evidence/v3-android-runtime-current-20260817.json`。该结果不替代客户授权物理真机，因此第 13 条及 V7 总状态保持“部分通过”。

第 13 条进一步补充：公开原生 `ubuntu-24.04` Runner 已用提交 `12df402ea2be04cc0fd8f47c5ca51c8b88b738a3` 在同一当前源码 Compose 栈连续运行 3600 秒代表性尺寸混合负载。绿色强制门禁证明三类认证 API 至少 32,400 次且错误为 0；Local Agent 至少完成 100 个带 32 MiB 随机不可压缩资产的合成 APK，源包到最终签名包的载荷 SHA-256 保持，累计源载荷至少 3,355,443,200 字节；临时 MinIO 至少完成 600 次 8 MiB Put/Stat/Get/SHA/Delete/NotFound，上传和下载各至少 5,033,164,800 字节；8 个核心容器零重启。Artifact 上传与合成平台清理均成功，证据见 `docs/enterprise/evidence/v7-representative-artifact-soak-hour-public-ci-20260817.json`。计数按同提交工作流的强制下限归档，不冒充 Artifact 内的精确计数；该单 Runner 合成小时证据仍不替代天级、客户峰值或生产容量，因此状态不变。

第 4 条协议兼容补充：提交 `28029165a04bf53e059bbeac8b23c2a08b0928d6` 已消除部署展示窗口与 Core 调度门禁的实现漂移。`deploy/acceptance/v7-agent-protocol-window.sh` 在本地 Docker MySQL 使用临时 Agent、证书、任务和额度记录，验证协议1/4保留安全心跳/轮换但拒绝新任务、协议2在线但拒绝协议3 Task Bundle、协议3正常领取和完成运行时生命周期、配置漂移拒绝及记录清理；未访问客户数据或生产凭据。0600 证据见 `docs/enterprise/evidence/v7-agent-protocol-window-20260817.json`。第 4 条保持通过，V7 总状态不变。

外部门禁补充：剩余客户/生产验收现统一映射到 8 个现场证据，详见 `docs/enterprise/customer-site-acceptance.md`。`deploy/production/assemble-customer-site-evidence.sh` 固定汇总门禁文件和 SHA，`validate-customer-site-evidence.sh` 强制客户/AppForge 双签、Schema 113、协议3、真实客户环境、零开放问题、敏感信息拒绝及各门禁指标；`deploy/acceptance/v7-customer-site-evidence-contract.sh` 已用临时双密钥通过正常路径，并确认摘要篡改、fixture 冒充客户环境、HSM 缺项、未脱敏端点和签名篡改全部拒绝。本地 0600 机器证据见 `docs/enterprise/evidence/v7-customer-site-evidence-contract-20260817.json`；公开原生 Linux CI 的契约、脱敏报告检查和 Artifact 上传也全部成功，见 `docs/enterprise/evidence/v7-customer-site-evidence-contract-public-ci-20260817.json`。该结果是交付流程能力，不是客户现场证据，因此 6 条通过、7 条部分通过的状态不变。

## 3. 已执行基础验收

以下验收在本地 Docker 环境通过：

- `deploy/acceptance/v4-build-cluster.sh` 的历史完整验收和 2026-08-15 缓存清理/真实 API 恢复后的当前复核均通过，证据见 `docs/enterprise/evidence/v4-build-cluster-current-20260815.json`
- `deploy/acceptance/v5-open-platform.sh`
- `deploy/acceptance/v6-commercialization.sh`
- `deploy/acceptance/v7-enterprise-delivery.sh`
- `deploy/acceptance/v7-agent-protocol-window.sh` 的协议1–4运行时窗口、配置漂移拒绝、证书轮换和真实 MySQL 任务领取门禁；证据见 `docs/enterprise/evidence/v7-agent-protocol-window-20260817.json`
- `deploy/acceptance/v7-customer-site-evidence-contract.sh` 的固定 8 门禁、Schema/协议边界、SHA、防敏感信息与客户/AppForge 双签正常/负向契约；仅证明现场证据工具，不代表客户门禁已执行
- `deploy/acceptance/v7-secret-providers.sh` 的本地文件和临时真实 Vault 场景
- `deploy/acceptance/local-agent-executor.sh` 的真实最小 APK 渠道注入、对齐、签名、包名和摘要校验
- `deploy/acceptance/local-agent-delivery.sh` 的无入站端口 Compose、固定非 root 用户、只读安全基线、私有 Secret 卷与拒绝任意字段验收
- `deploy/acceptance/v7-diagnostics.sh` 的诊断最小采集、文件权限、SHA 清单和敏感值扫描
- `deploy/acceptance/v7-production-preflight.sh` 的生产配置渲染、Compose 解析和私有文件权限拒绝门禁
- `deploy/acceptance/v7-offline-layout.sh` 的离线 SHA、OCI 导入、控制面文件、CLI、Local Agent 安装包和企业文档落盘
- `deploy/acceptance/v7-offline-install.sh` 的预载镜像、禁止拉取、双 internal 网络、内部 TLS/API 登录、外部 HTTPS 拒绝、空卷 Schema 113、非 root、离线许可证与私有对象存储本地模拟断网安装；证据见 `docs/enterprise/evidence/v7-offline-install-20260815.json`
- `deploy/acceptance/v7-offline-upgrade.sh` 的预载应用镜像 `1.2.0 → 1.2.1 → 1.2.0` 离线升级/回滚、登录恢复、Schema 113 数据库/对象标记保持及连续健康探针中断门禁；证据见 `docs/enterprise/evidence/v7-offline-upgrade-20260815.json`
- `deploy/acceptance/v7-formal-offline-upgrade.sh` 的双正式 tag/OIDC/Cosign/介质 SHA、不同 Git commit、16 个不同 AppForge 平台摘要、发布部署文件切换、目标迁移镜像、应用与 MySQL/etcd/MinIO 实际升级/回滚、数据库/对象保持及连续健康探针门禁；证据见 `docs/enterprise/evidence/v7-formal-offline-upgrade-v1.2.5-v1.2.6-20260817.json`
- `deploy/acceptance/v7-release-workflow.sh` 的 tag 限定、语义化镜像标签、digest 证据链和 Linux 双架构 CLI 构建
- `common/siem` 的 HTTPS Webhook 与 RFC5424/RFC6587 Syslog TLS：真实 TLS、自定义 CA、Bearer Token 文件热轮换、HTTP 503 重试、Syslog 结构化字段/八位计数帧、逐事件重连、受限 CONNECT 隧道、明文与危险 Endpoint 拒绝及 race test；Syslog 证据见 `docs/enterprise/evidence/v7-syslog-tls-20260817.json`
- `deploy/acceptance/v7-network-policy.sh` 的按组件/CIDR/端口 Helm 渲染、Schema 负向门禁和关闭开关检查
- `deploy/acceptance/v7-observability.sh` 的真实 go-zero Prometheus HTTP 请求计数/耗时、OTLP/HTTP Trace 批次和生产明文 Endpoint 拒绝；0600 证据见 `docs/enterprise/evidence/v7-observability-20260815.json`
- `deploy/acceptance/v7-network-policy-runtime.sh` 的 Calico v3.32.1/Kind v1.32.2 真实允许/拒绝连通性验收，临时集群已自动清理
- `deploy/acceptance/v7-backup-restore.sh` 的独立 MySQL、etcd、对象数据破坏后恢复、一致性核对、RPO/RTO 门禁和 0600 JSON 报告
- `deploy/acceptance/v7-mysql-pitr.sh` 的同点 dump/binlog 坐标、同版本工具镜像、关闭日志归档与 SHA、有意删除测试表后的 UTC 截止时间恢复、截止后事件排除、RTO 门禁和 Docker 资源零残留；证据见 `docs/enterprise/evidence/v7-mysql-pitr-20260815.json`
- `deploy/acceptance/v7-object-replication.sh` 的两个独立 MinIO 数据卷、双对象版本与 SHA、删除标记复制、源容器/卷销毁、副本版本恢复和 Docker 资源零残留；证据见 `docs/enterprise/evidence/v7-object-replication-20260815.json`，不代表真实跨区域故障域
- `deploy/acceptance/v7-private-install.sh` 的全新 Private 空数据卷安装、迁移、固定 UID、TLS 双前端、许可证持久状态、启动管理员登录和私有 MinIO Bucket 初始化
- `deploy/acceptance/v7-dedicated-install.sh` 的全新 Dedicated 空数据卷安装和 Dedicated 模式许可证绑定
- `deploy/acceptance/v7-local-agent-recovery.sh` 的真实 Agent 临期证书自动轮换、轮换证书吊销后拒绝、真实 APK 执行中强制中断、attempt 恢复、旧 attempt 拒绝、状态卷连续、离线镜像升级和不健康版本自动回滚
- `deploy/acceptance/v7-control-plane-storage.sh` 的临时真实 APK/Keystore、mTLS 双向认证、证书绑定单次票据、防重放、上传后篡改拒绝、attempt 恢复、真实 APK/日志回传、控制面字节级 SHA 复核和 Core 对象绑定
- `deploy/acceptance/v7-customer-storage.sh` 的受限临时 MinIO、合成 APK/Keystore 客户侧导入、mTLS 登记、幂等冲突、证书/前缀拒绝、输入篡改拒绝、attempt fencing、真实 APK/日志客户侧回读、对象归属、控制面零客户字节和 Secret 防泄漏
- `deploy/acceptance/v7-customer-storage-site-probe.sh` 的正式 Local Agent MinIO Provider 现场探针夹具：随机合成对象 Put/Stat/完整 Get SHA-256/Delete/权威 NotFound、预置既有对象不变、合成对象零残留、endpoint/bucket/prefix/凭据脱敏和 Docker 临时资源零残留；证据明确标记 `environmentKind=fixture`，见 `docs/enterprise/evidence/v7-customer-storage-probe-fixture-20260817.json`，不代表真实客户 S3/OSS
- `deploy/acceptance/v7-air-gapped.sh` 的租户门户路由、正式最小 RBAC 目录、未授权 403/专用角色显式授权、临时租户/证书/私有卷、合成 APK/Keystore、任务与额度锁定、双向签名 ZIP、错误 Agent 与双向篡改拒绝、`--network none` 构建、本地防重放、事务导入、幂等、最终 APK 签名/包名校验及清理归零；0600 机器证据见 `docs/enterprise/evidence/v7-air-gapped-20260815.json`
- `deploy/acceptance/v7-schema113-candidate.sh`（历史文件名）的 112/113 双迁移镜像边界、113 生产空库安装、112→113 升级、重复迁移、升级前数据保持、六条固定 RBAC 和已有角色零自动授权；0600 机器证据见 `docs/enterprise/evidence/v7-schema113-production-20260816.json`
- `deploy/acceptance/v7-npm-audit.sh` 的 npm 官方生产依赖与全部依赖零漏洞门禁；Vite 8.2.1、Vue 插件 6.0.8、Node 20.20.2，证据不导出依赖明细，0600 机器证据见 `docs/enterprise/evidence/v7-npm-audit-20260816.json`
- `deploy/acceptance/v7-remote-apk-signing.sh` 的临时 mTLS CA/客户端身份、合成 APK/测试 Keystore、固定远程签名协议、请求与响应篡改拒绝、错误客户端/keyId/证书拒绝、超时无产物、服务重启后持久防重放和最终 APK 签名证书校验；证据见 `docs/enterprise/evidence/v7-remote-apk-signing-20260815.json`，不代表真实 HSM
- `deploy/acceptance/v7-remote-signing-task-e2e.sh` 的唯一临时 Compose 栈、公共管理 API 远程签名配置、Schema 112 兼容持久化、创建时 mTLS 身份校验、无能力节点调度拒绝、真实 Worker 远程签名、最终 APK/渠道资产校验、零 Keystore/零密码、Secret 泄漏扫描及容器/卷/网络零残留；证据见 `docs/enterprise/evidence/v7-remote-signing-task-20260815.json`，不代表真实 HSM
- `deploy/acceptance/v7-release-workflow.sh` 的 tag-only 工作流、源码依赖无明细导出 High/Critical 门禁及失败阻断、16 个 AppForge 发布镜像与两个原始第三方镜像 digest 引用、SPDX、标准化许可证清单、Trivy SARIF、AppForge 逐镜像 Cosign 回验记录、18 镜像聚合发布清单、公开 Release 固定资产、SHA 篡改拒绝和双架构 CLI 构建；合成契约证据见 `docs/enterprise/evidence/v7-release-evidence-contract-20260816.json`，`v1.2.6` 真实 tag/OIDC/Trivy/Cosign 22/22 任务及匿名双层签名验证证据见 `docs/enterprise/evidence/v7-public-release-v1.2.6-20260816.json`，正式介质本地断网全新安装证据见 `docs/enterprise/evidence/v7-formal-offline-media-v1.2.6-20260816.json`
- Local Agent 发布版本一致性门禁：二进制由 `APPFORGE_VERSION`/`-X main.version` 注入 tag metadata，构建参数拒绝非语义化版本，发布工作流实际启动 digest 镜像并逐字核对版本与协议；公开原生 `linux/amd64` 客户存储探针 CI 已成功，`v1.2.7` 正式发布 22/22 作业成功，正式 digest 镜像由远端门禁与本机 `linux/amd64` 仿真独立确认输出 `appforge-local-agent 1.2.7 protocol=3`，公开归档 SHA-256 清单通过，证据见 `docs/enterprise/evidence/v7-customer-storage-probe-release-v1.2.7-20260817.json`
- `deploy/acceptance/v7-kubernetes-upgrade.sh` 的临时 kind 集群、Schema 113 Helm 安装、`1.2.0 → 1.2.1 → 1.2.0` 同摘要快速回归、连续 API 零失败探测、滚动升级、应用回滚、双 API 副本及 AIR_GAPPED 表保持；证据见 `docs/enterprise/evidence/v7-kubernetes-upgrade-20260816.json`
- `deploy/acceptance/v7-formal-kubernetes-upgrade.sh` 的双正式 tag 聚合 Sigstore、21 镜像实时 Cosign、原生 amd64 架构、9/9 个不同应用镜像摘要、`1.2.5 → 1.2.6 → 1.2.5` Helm 滚动升级/应用回滚、571 次连续探测零失败及 Schema 113/AIR_GAPPED/双 API 副本保持；证据见 `docs/enterprise/evidence/v7-formal-kubernetes-upgrade-v1.2.5-v1.2.6-20260817.json`
- `deploy/acceptance/v1-v7-regression-base.sh` 的 11 个 Go 模块、Node 20 前端、全部交付 Shell、V2/V3 当前只读复核、V4–V7 运行验收、隔离远程签名完整任务 E2E 和限定规模性能冒烟；V3 Android 当前 API 29 ARM64 Emulator 安装/启动/UI/原位升级证据见 `docs/enterprise/evidence/v3-android-runtime-current-20260817.json`，客户授权物理真机仍是 V7 独立门禁
- `deploy/acceptance/v4-builder-recovery.sh` 仅针对三个固定开发节点，经真实管理员登录、RBAC、HTTP、Core RPC 和恢复安全前置检查解除人工隔离，不直接更新数据库
- `deploy/acceptance/v7-performance-smoke.sh` 的 healthz 500 次/32 并发和部署状态 150 次/12 并发零错误 P95 门禁；结果仅代表本地冒烟
- `deploy/acceptance/v7-artifact-capacity.sh` 的 4 个合成真实 APK 并发构建、临时 MinIO 128 MiB 双向吞吐与 SHA-256 校验、8 并发 120 秒 API 稳定性门禁；0600 机器可读证据见 `docs/enterprise/evidence/v7-artifact-capacity-20260815.json`，结果仅代表单开发机本地合成容量
- `deploy/acceptance/v7-api-soak.sh` 与 `.github/workflows/hourly-api-soak.yml` 的公开原生 `linux/amd64` 当前源码 Compose 3600 秒认证 API 长稳：health/ready 及 JWT/RBAC/RPC/数据库部署状态探针满足独立 P99、最小请求数、零错误和零连续错误门禁，8 个核心容器运行且零重启，Artifact `hourly-api-soak-31969588517` 已上传并完成合成平台清理；证据见 `docs/enterprise/evidence/v7-api-soak-hour-public-ci-20260817.json`，不代表天级、APK/对象吞吐或客户峰值
- `deploy/acceptance/v7-artifact-soak.sh` 与增强后的 `.github/workflows/hourly-api-soak.yml` 在公开原生 `linux/amd64` 当前源码栈并行运行认证 API、Local Agent 合成 APK 打包/签名和临时 MinIO 对象往返 3600 秒；API/构建/对象错误均为 0，至少 120 次 APK 构建、600 次 1 MiB Put/Stat/Get/SHA/Delete/NotFound、每方向至少 600 MiB，8 个核心容器零重启，Artifact `hourly-api-soak-31973439491` 上传和合成资源清理成功；证据见 `docs/enterprise/evidence/v7-artifact-mixed-soak-hour-public-ci-20260817.json`，不代表天级、客户峰值或生产规模容量
- 代表性尺寸混合门禁继续使用上述脚本和公开原生 `ubuntu-24.04` 当前源码栈：固定执行器逐字节验证 32 MiB 随机载荷从源 APK 保持到最终签名 APK；至少 100 次 APK 构建、600 次 8 MiB 对象完整往返、每方向至少 5,033,164,800 字节和三类认证 API 至少 32,400 次，错误均为 0，8 个核心容器零重启。Run `31977504252`、Artifact `hourly-api-soak-31977504252` 的上传和平台清理成功；证据见 `docs/enterprise/evidence/v7-representative-artifact-soak-hour-public-ci-20260817.json`，计数为同提交工作流强制下限，不代表天级或客户生产容量
- 受 RBAC 保护的“部署与升级”页面真实登录验收：未认证请求拒绝，API/System/Core/Builder/MySQL 五项实际健康，许可证公开状态、113 实际迁移和升级前置条件一致；页面不执行服务器命令
- Local Agent race test、PKI/真实 MySQL 协议3注册/轮换/吊销/Drain/租约恢复/fencing 测试、离线许可证 CLI、Compose config、Shell syntax、values Schema 和 Helm strict lint
- 管理员已在磁盘双门禁恢复后通过真实 API 恢复三个隔离 Builder 节点，验证登录、RBAC、HTTP、Core RPC、事务和调度事件链路；当前三节点在线接单并通过 V4 不变量复核

`deploy/acceptance/v7-appforge-schema112-dr.sh` 已完成历史 1.1.x/Schema 112 的兼容恢复运行：严格排除 Schema 113，仅装载历史目标 Schema 112，核对代表性合成租户、应用、版本、渠道、签名配置、成功构建任务、操作审计、四类对象元数据/字节和 etcd 配置；备份后销毁整个隔离数据库、etcd 前缀和精确对象目录，恢复后摘要一致，RPO 2 秒、RTO 10 秒。0600 机器证据见 `docs/enterprise/evidence/v7-appforge-schema112-dr-20260815.json`。

`deploy/acceptance/v7-appforge-schema113-dr.sh` 已完成当前 1.2.x/Schema 113 的生产边界恢复运行：在上述代表性业务链上增加 AIR_GAPPED 已导入状态、离线任务包/结果包对象与 task/attempt 引用校验，销毁恢复后六个对象逐字节摘要、58 张 AppForge 表、业务摘要和 etcd 配置一致，RPO 2 秒、RTO 11 秒。0600 机器证据见 `docs/enterprise/evidence/v7-appforge-schema113-dr-20260816.json`。

基础脚本末尾明确声明：通过的是“V7 企业交付基础验收”；三种 Artifact 模式、Schema 113 生产迁移、最小 RBAC、正式 tag 安全发布及正式 Kubernetes 双版本升级/回滚已有独立证据，但不代表真实 AWS/S3/OSS、客户物理断网介质、客户目标 Kubernetes 或生产规模业务灾备已经通过。

## 4. 关键实现边界

### 4.1 Local Agent

当前 Agent 能安全注册、维持 mTLS 身份、领取任务、续租并提交结果。真实 Agent 的临期证书自动轮换、本地原子切换、吊销后拒绝与管理端到期告警已有运行证据。协议3使用白名单 Task Manifest，并实现客户侧 `local-file://` 签名和对象存储 Secret Provider。客户交付包不开放入站端口，以固定非 root 用户和只读根文件系统运行。固定程序 `appforge-local-build` 不接受 shell 或任意命令，输入及输出路径、普通文件、私有权限、大小和 SHA 均强制校验；对象存储凭据、控制面 Secret 和传输票据不会转交严格执行器。`CONTROL_PLANE_STORAGE` 与 `CUSTOMER_STORAGE` 均已完成真实 APK/Keystore、构建、APK/日志和恢复 E2E。

### 4.2 Artifact

`CONTROL_PLANE_STORAGE` 已形成两层校验。Gateway 的 Redis 状态只保存带 TTL 的票据哈希和 task/attempt 绑定；下载/上传票据绑定当前 mTLS 证书且原子单次消费。Agent 对下载输入校验大小和 SHA，Gateway 对上传流首次计算 SHA，并在完成前再次从私有对象存储读取全部字节独立重算；随后 Core 强制规范 `storage-object://ID`，校验 tenant、app、Artifact/对象类型、READY/BOUND 状态、大小、SHA、task/attempt 和对象复用冲突，成功后绑定 BOUND 并回填任务 APK/日志对象 ID。

`CUSTOMER_STORAGE` 已按 [实施契约](./docs/enterprise/customer-storage-contract.md) 完成：不新增代理前端，客户 APK/Keystore 由受限 Agent 本地导入命令直接进入 S3/MinIO/OSS；控制面仅登记显式归属、规范引用和完整性元数据。`AIR_GAPPED` 已按 [实施契约](./docs/enterprise/air-gapped-artifact-contract.md) 完成控制面导出、离线固定构建、Agent 签名结果和控制面导入。两种 E2E 均在限定授权内使用合成数据并自动清理，不等同于真实 S3/OSS、客户介质或生产权限验收。

### 4.3 企业 Secret

企业 Secret Resolver 已接入普通 Builder Worker 的签名阶段，并非孤立包。当前支持 local-file、Kubernetes 投射文件、Vault 和 AWS Secrets Manager；Local Agent 还支持严格目录约束、拒绝符号链接和私有权限校验的 `local-file://` Secret Store。远程 APK 签名已按 [固定契约](./docs/enterprise/remote-apk-signing-contract.md) 接入管理 API、Core 持久化/调度和 Builder 真实任务分支，并有协议级与任务级合成 E2E；这仍不能替代客户 HSM 的不可导出密钥、HA、审计和性能验收。

## 5. 后续落地顺序

1. 按 [Local Agent Task Bundle 与 Artifact 协议](./docs/enterprise/local-agent-artifact-protocol.md) 持续保留 `CONTROL_PLANE_STORAGE` 的容器与客户环境回归。
2. 在客户测试环境复验 `CUSTOMER_STORAGE` 的真实 S3/阿里云 OSS 最小权限和工作负载身份，并复验 `AIR_GAPPED` 的物理介质、正式证书/镜像和最小 RBAC。
3. 按 [受限网络出口与企业代理契约](./docs/enterprise/restricted-egress-proxy.md) 填写生产域名/IP allowlist，现场复验代理容量、DNS/CNI/防火墙；远程签名与 Webhook 使用独立直连规则，不得通过通用代理或开放全后端出口。
4. 完成真实 AWS 和客户物理断网安装/升级；在客户测试 HSM 上复验现有远程签名任务分支的不可导出密钥、PIN/会话、HA、审计、限流与性能；按客户需求决定 SSO/SCIM 范围。正式发布流水线与匿名 Release 资产验证继续作为每个 tag 的持续门禁。
5. 在获得现有业务数据隔离复制授权后，使用已实现的基线恢复和 PITR 工具执行生产规模业务摘要核对、异地副本恢复，并完成客户 RPO/RTO 计时演练。
6. 在其余缺口闭环后，使用已实现的部署健康/版本/许可证/升级页面执行全量 V7 验收。
