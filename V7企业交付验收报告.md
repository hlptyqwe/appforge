# AppForge V7 企业交付验收报告

## 1. 结论

验收日期：2026-08-15。

V7 当前为“实施中”，不能标记完成。13 条严格验收中，5 条已通过，8 条部分通过，0 条完全未验收。V1–V6 已完成，因此《APK 渠道动态打包平台方案》的主 SaaS 产品闭环完整；企业级 Dedicated、Private 和 Hybrid 交付闭环仍有缺口。

状态定义：

- `通过`：该条全部要求已有实际运行证据。
- `部分通过`：已有实现和局部运行证据，但端到端或目标环境证据不完整。
- `未验收`：尚未执行该条要求的完整实演；已有脚本或配置不能替代验收。

## 2. 严格验收矩阵

| # | 状态 | 已有实现与证据 | 未完成项 |
| --- | --- | --- | --- |
| 1 | 通过 | Compose、Helm、values Schema、生产 preflight、迁移 Job 均已实现；已用当前源码构建固定版本镜像，分别在独立空数据卷 Private 与 Dedicated Compose 环境一次完成迁移、固定非 root UID、TLS 双前端、对应模式离线许可证状态、启动管理员登录和私有对象存储 Bucket 初始化；全程使用临时随机密钥且无开发默认凭据 | 持续在正式发布镜像与客户 Linux 环境保留安装回归 |
| 2 | 通过 | 在一次性 kind v1.32.2 集群以 Helm 3.17.3 完成固定镜像安装、`1.1.0 → 1.1.1` RollingUpdate 和 revision 1 应用回滚；API 与 UI 按生产最小副本运行，集群内探针在升级/回滚全程连续请求 `/healthz` 无失败；回滚后 1.1.0 API 双副本可用，110 expand Schema 保留并兼容旧应用；验收 namespace、静态 RWX 测试卷和 kind 集群均已清理 | 持续在客户目标 Kubernetes/CSI/Ingress 环境保留同等回归；现有 RWX 许可证 Claim 必须预置 UID/GID 65532 写权限 |
| 3 | 部分通过 | 已在独立临时 Compose 环境完成 MySQL、etcd、对象数据的一致性备份，删除数据后恢复并逐项核对；计时演练得到 RPO 2 秒、RTO 9 秒，均低于参考目标 900/7200 秒并生成 0600 机器可读报告；修复 MinIO 无 `tar`、distroless etcd 无 shell、MySQL socket 隐式依赖及迁移字符串 `MAX(version)` 排序错误 | 尚未使用完整 AppForge 业务库恢复并核对租户、任务、对象引用、操作审计及迁移集合；复制现有开发业务数据到隔离环境前需要明确数据导出授权 |
| 4 | 部分通过 | Local Agent 使用主动出站 HTTP/mTLS 协议注册、心跳、领取和回报；协议3白名单 Task Bundle、客户侧安全 Compose/安装升级包、本地 Secret 和仓库内固定 `appforge-local-build` 已实现；Docker 内真实完成最小 APK 渠道注入、对齐、签名和校验 | APK/Keystore 输入传输和构建产物上传尚未接通，仍未完成控制面到客户 Agent 的真实任务 E2E |
| 5 | 通过 | 一次性注册码只存 SHA-256，管理端安装命令通过隐藏输入和 `--token-stdin` 传递注册码；Agent 私钥只在本地。真实 MySQL 验收已覆盖注册、证书签发、轮换后旧证书拒绝和管理员吊销后新证书拒绝；隔离 mTLS Gateway 下的真实 Agent 已检测临近到期证书、生成新私钥并自动轮换，原子切换本地证书后继续通信，Gateway 吊销开关随即拒绝当前轮换证书并记录证书序列号；管理页面显示已过期和 24 小时内到期告警 | 持续在正式环境保留长周期轮换、吊销和告警回归，并按客户要求接入外部告警通道 |
| 6 | 通过 | 协议只暴露固定任务操作，不提供 shell/任意命令；领取 SQL 强制 tenant、pool 和 allowed app；跨租户数据完整性检查与真实 MySQL 测试通过 | 持续保留跨租户与任意命令回归门禁 |
| 7 | 部分通过 | 三种 Artifact 模式已有枚举；控制面校验 Agent 授权模式、租户、任务归属、builder attempt、引用格式、声明 SHA 和大小 | 尚未完成三种模式的真实 APK E2E；控制面当前不读取对象字节独立重算 SHA |
| 8 | 通过 | 真实 MySQL 故障注入已验证租约恢复、attempt fencing 和 Drain；隔离 mTLS Gateway 下，真实 APK 执行器在 attempt 1 运行中被 `SIGKILL`，新容器复用私有状态卷并以 attempt 2 完成签名 APK，防重放时间戳跨进程持续增长，旧 attempt 完成请求返回 409；已导入固定镜像的离线升级、状态卷保留、不健康镜像自动回滚和旧镜像健康复核均已实测通过 | Artifact 传输票据及真实上传仍归第 4、7 条，不能用本条合成输入验收替代 |
| 9 | 部分通过 | Builder Worker 已实际接入 local-file、k8s-secret、Vault、AWS Secrets Manager；Local Agent 已实现受限 `local-file://` Secret Store；本地文件和临时真实 Vault 通过运行验收 | AWS 只有单元测试，尚缺客户测试账户/工作负载身份真实验收；HSM/远程签名未实现 |
| 10 | 部分通过 | 离线镜像导入脚本和离线签名许可证已实现；许可证生成、部署绑定、持久防回拨、OCI 导入、Local Agent 安装包、CLI 和企业文档落盘校验通过 | 尚未在物理断网环境完成全新安装和升级，也未把真实 tag 的 SBOM/签名/漏洞证据归档进正式离线介质 |
| 11 | 部分通过 | 发布工作流按镜像 digest 生成 SBOM、执行 Trivy High/Critical 门禁并归档 SARIF、Cosign keyless 签名后回验；CLI 校验和带 Sigstore 证明；Helm 新增按组件、CIDR、协议和端口限定的严格出口规则，Schema 拒绝缺少端口的配置；Calico v3.32.1/Kind v1.32.2 已真实验证指定端口放行、同 CIDR 未授权端口拒绝、未授权组件拒绝、内部通信和入口 Namespace 隔离，证据见 `docs/enterprise/evidence/v7-network-policy-20260815.json`；SIEM HTTPS exporter 已通过真实 TLS 和 `-race` 验收 | 尚无正式 tag 流水线产物、客户目标 CNI/Ingress/最终 allowlist 复验和客户 SIEM 端点联调记录；Compose 后端网络为 `internal`，支付、Webhook、代码平台、外部对象存储/Vault/AWS 所需出站 allowlist 尚未获授权配置；兼容字段 `egressCIDRs` 仍会放行目标 CIDR 全端口，新交付禁止使用；syslog 未实现 |
| 12 | 部分通过 | 隔离合成 MySQL、etcd、对象数据已完成破坏恢复计时：RPO 2 秒/目标 900 秒，RTO 9 秒/目标 7200 秒，三类恢复数据一致；报告见 `docs/enterprise/evidence/v7-synthetic-dr-20260815.json`。kind/Helm 已完成应用升级和回滚且连续探针零失败 | 合成数据不代表完整业务库或客户目标基础设施；仍需完整业务摘要、PITR/跨区域恢复、客户 RPO/RTO 计时和正式升级回滚演练记录 |
| 13 | 部分通过 | `deploy/acceptance/v1-v7-regression-base.sh` 已在同一次运行中通过 11 个 Go 模块全量测试/编译、Node v20.20.2 前端类型检查、全部交付 Shell 语法以及 V4 构建集群、V5 开放平台、V6 商业化、V7 企业交付运行验收；本地性能冒烟以 32 并发完成 500 次 healthz、12 并发完成 150 次带 JWT/RBAC/RPC/数据库读取的部署状态请求，均 0 错误，P95 分别为 30.928ms/门禁250ms、26.213ms/门禁1000ms；回归同时修复 Node 版本漂移、Builder 隔离恢复前置和 secretbox 篡改测试随机误报，证据见 `docs/enterprise/evidence/v1-v7-base-regression-20260815.json` 与 `v7-performance-smoke-20260815.json` | 未执行 Android 授权真机、客户容量/峰值、APK 构建吞吐、对象存储吞吐、长稳和剩余外部集成；三种 Artifact E2E、真实 AWS、客户 SIEM、正式发布流水线等缺口未闭环前不能标记全量通过 |

## 3. 已执行基础验收

以下验收在本地 Docker 环境通过：

- `deploy/acceptance/v4-build-cluster.sh`
- `deploy/acceptance/v5-open-platform.sh`
- `deploy/acceptance/v6-commercialization.sh`
- `deploy/acceptance/v7-enterprise-delivery.sh`
- `deploy/acceptance/v7-secret-providers.sh` 的本地文件和临时真实 Vault 场景
- `deploy/acceptance/local-agent-executor.sh` 的真实最小 APK 渠道注入、对齐、签名、包名和摘要校验
- `deploy/acceptance/local-agent-delivery.sh` 的无入站端口 Compose、固定非 root 用户、只读安全基线、私有 Secret 卷与拒绝任意字段验收
- `deploy/acceptance/v7-diagnostics.sh` 的诊断最小采集、文件权限、SHA 清单和敏感值扫描
- `deploy/acceptance/v7-production-preflight.sh` 的生产配置渲染、Compose 解析和私有文件权限拒绝门禁
- `deploy/acceptance/v7-offline-layout.sh` 的离线 SHA、OCI 导入、控制面文件、CLI、Local Agent 安装包和企业文档落盘
- `deploy/acceptance/v7-release-workflow.sh` 的 tag 限定、语义化镜像标签、digest 证据链和 Linux 双架构 CLI 构建
- `common/siem` 的真实 TLS、自定义 CA、Bearer Token 文件热轮换、HTTP 503 重试和 race test
- `deploy/acceptance/v7-network-policy.sh` 的按组件/CIDR/端口 Helm 渲染、Schema 负向门禁和关闭开关检查
- `deploy/acceptance/v7-network-policy-runtime.sh` 的 Calico v3.32.1/Kind v1.32.2 真实允许/拒绝连通性验收，临时集群已自动清理
- `deploy/acceptance/v7-backup-restore.sh` 的独立 MySQL、etcd、对象数据破坏后恢复、一致性核对、RPO/RTO 门禁和 0600 JSON 报告
- `deploy/acceptance/v7-private-install.sh` 的全新 Private 空数据卷安装、迁移、固定 UID、TLS 双前端、许可证持久状态、启动管理员登录和私有 MinIO Bucket 初始化
- `deploy/acceptance/v7-dedicated-install.sh` 的全新 Dedicated 空数据卷安装和 Dedicated 模式许可证绑定
- `deploy/acceptance/v7-local-agent-recovery.sh` 的真实 Agent 临期证书自动轮换、轮换证书吊销后拒绝、真实 APK 执行中强制中断、attempt 恢复、旧 attempt 拒绝、状态卷连续、离线镜像升级和不健康版本自动回滚
- `deploy/acceptance/v7-kubernetes-upgrade.sh` 的临时 kind 集群、Helm 安装、连续 API 探测、滚动升级、应用回滚和 expand Schema 兼容验收
- `deploy/acceptance/v1-v7-regression-base.sh` 的 11 个 Go 模块、Node 20 前端、Shell 和 V4–V7 单次统一基础回归；V3 真机与性能测试明确不在通过范围
- `deploy/acceptance/v4-builder-recovery.sh` 仅针对三个固定开发节点，经真实管理员登录、RBAC、HTTP、Core RPC 和恢复安全前置检查解除人工隔离，不直接更新数据库
- `deploy/acceptance/v7-performance-smoke.sh` 的 healthz 500 次/32 并发和部署状态 150 次/12 并发零错误 P95 门禁；结果仅代表本地冒烟
- 受 RBAC 保护的“部署与升级”页面真实登录验收：未认证请求拒绝，API/System/Core/Builder/MySQL 五项实际健康，许可证公开状态、111 实际迁移和升级前置条件一致；页面不执行服务器命令
- Local Agent race test、PKI/真实 MySQL 协议3注册/轮换/吊销/Drain/租约恢复/fencing 测试、离线许可证 CLI、Compose config、Shell syntax、values Schema 和 Helm strict lint
- 管理员通过真实 API 恢复三个隔离 Builder 节点，验证登录、RBAC、HTTP、Core RPC、事务和调度事件链路

基础脚本末尾明确声明：通过的是“V7 企业交付基础验收”，不代表三种 Artifact、真实 AWS、完整业务灾备或正式发布证据已经通过。

## 4. 关键实现边界

### 4.1 Local Agent

当前 Agent 能安全注册、维持 mTLS 身份、领取任务、续租并提交结果引用。真实 Agent 的临期证书自动轮换、本地原子切换、吊销后拒绝与管理端到期告警已有运行证据。协议3已增加不含控制面密码密文的白名单 Task Manifest，并实现客户侧 `local-file://` 签名 Secret Provider；不安全的签名配置和白标敏感参数会被结构化阻断。客户交付包不开放入站端口，以固定非 root 用户和只读根文件系统运行，注册码通过标准输入传递，并支持本地健康检查、Drain 后升级、已导入镜像离线升级和失败回退。仓库内固定程序 `appforge-local-build` 已实现并通过 Docker 真实 APK 构建及进程中断恢复验收，任务 JSON 不支持 shell 或任意命令，输入路径、权限、大小和 SHA 均强制校验；任务结构中的 `version_id` 已补齐，避免 Agent 转交执行器时丢失版本身份。APK/Keystore 输入传输和构建产物上传仍未启用，因此当前属于“安全控制面、客户运行包、Manifest 与固定执行器已实现”，不是“控制面到客户网络的真实 APK E2E 已完成”。

### 4.2 Artifact

`VerifyHybridArtifact` 验证提交者身份、授权存储模式、任务 attempt、对象引用格式、声明 SHA 和大小，并记录元数据。它不会访问对象存储读取字节并重新计算 SHA。三种模式必须分别增加真实数据搬运和独立完整性验证后才能通过第 7 条。

### 4.3 企业 Secret

企业 Secret Resolver 已接入普通 Builder Worker 的签名阶段，并非孤立包。当前支持 local-file、Kubernetes 投射文件、Vault 和 AWS Secrets Manager；Local Agent 还支持严格目录约束、拒绝符号链接和私有权限校验的 `local-file://` Secret Store。HSM/远程签名尚未实现，文档不得提前声明支持。

## 5. 后续落地顺序

1. 在明确数据传输授权后，按 [Local Agent Task Bundle 与 Artifact 协议](./docs/enterprise/local-agent-artifact-protocol.md) 补齐 `CONTROL_PLANE_STORAGE` 构建输入、短时传输票据、产物上传和控制面字节级复验。
2. 再依次完成 `CUSTOMER_STORAGE`、`AIR_GAPPED` 的上传下载、字节级 SHA 校验和真实 APK E2E。
3. 明确生产出站域名/IP allowlist 后，仅为支付、Webhook、代码平台、客户对象存储和 Secret Provider 对应进程开放受控出站。
4. 完成真实 AWS、物理断网安装/升级和正式发布流水线证据；按客户需求决定 HSM 与 SSO/SCIM 范围。
5. 在获得现有业务数据隔离复制授权后执行完整业务恢复摘要核对，并完成 RPO/RTO 计时演练。
6. 在其余缺口闭环后，使用已实现的部署健康/版本/许可证/升级页面执行全量 V7 验收。
