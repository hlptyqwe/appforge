-- ============================================================================
-- APK Builder 数据库说明
--
-- V1 Builder 不拥有独立业务表。构建任务由 services/core/core.sql 中的
-- t_build_task 统一持有，Builder 通过 builder RPC 领取、续租和回报任务。
-- 这样可以避免 Core 与 Builder 对任务状态产生双重写入边界。
-- ============================================================================
