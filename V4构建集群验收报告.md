# AppForge V4 构建集群验收报告

## 1. 当前结论

验收日期：2026-08-14。

V4 的节点模型、公平调度、四级并发、任务取消/重试、租约恢复与 fencing、输入可寻址缓存、TTL/LRU、集群指标、管理页面、权限和 Docker 多 Worker 部署均已落地。规格中的 12 项验收标准已有 2026-08-14 实际运行证据；2026-08-15 清理可重建 BuildKit 缓存并通过真实管理 API 恢复三个固定节点后，当前只读验收再次全项通过，状态恢复为 `完成`。

仓库提供只读复核脚本 [v4-build-cluster.sh](./deploy/acceptance/v4-build-cluster.sh)，用于检查节点、策略、事件、槽位、产物一致性和租户隔离不变量。

2026-08-15 曾发现 Docker VM 最小可用空间仅 `501182464` 字节、约 478 MiB / `0.80%`，三个节点按 512 MiB 与 2% 双门禁进入隔离。获批执行 `docker builder prune -f --filter until=4h` 后释放约 `2.435GB` 可重建缓存，节点重新上报最小约 `3.05GB / 4.864%`；随后通过登录、RBAC、HTTP、Core RPC 和恢复前置检查调用真实管理 API，三个节点恢复在线接单。当前证据中的三节点、调度事件、槽位、产物和租户隔离检查均通过。

品牌预检领取现已强制检查同一节点的在线/Drain/心跳/并发/磁盘/工具链/协议和 `branding` capability；拒绝分支单测通过，修复已随新 Core 镜像 `sha256:72fff4ce62c89cbece2346baa0904eaa7948a6c5be9ded893ff0360d86e249d4` 部署。运行日志确认 `ClaimBrandingPreflight` 在查询待领取预检前先锁定对应 `t_builder_node`，当前合格 branding 节点计数为 3。

## 2. 调度与并发证据

### 2.1 三节点并行构建

任务 `20`、`21`、`22` 在 `2026-08-14 17:26:20` 同时开始，分别由 `builder-dev-03`、`builder-dev-02`、`builder-dev-01` 执行并成功完成。槽位表的 `(task_id, builder_attempt)` 唯一约束和验收查询均确认没有重复领取同一代次。

### 2.2 租户、应用和全局并发

- 租户与应用上限为 `1` 时，任务按优先级 `25 → 24 → 23` 串行执行，领取事件记录 `tenantLimit=1`、`appLimit=1`。
- 全局上限临时设为 `1`、租户和应用上限设为 `3` 时，三个真实且未命中缓存的任务严格串行：任务 `39` 为 `17:57:43–17:57:45`，任务 `40` 为 `17:57:45–17:57:47`，任务 `38` 为 `17:57:47–17:57:49`。每次 `FAIR_QUEUE_CLAIM` 均记录 `globalLimit=1`。
- 验收后策略已恢复为全局 `10`、租户 `3`、应用 `3`。

### 2.3 公平队列与优先级

隔离验收节点依次领取：租户 1 优先级 100、租户 2 优先级 1、租户 1 优先级 99，顺序为 `34 → 36 → 35`。这证明租户内高优先级有效，同时低优先级租户不会被同一租户连续高优先级任务饿死。合成任务、槽位、事件、租户 2 队列和隔离节点已在记录证据后精确删除，不再污染业务列表，删除不可恢复。

## 3. 节点、取消与恢复证据

1. Drain 验收中，节点 `builder-dev-01` 领取任务 `26` 后进入排空，已有任务继续完成；排空期间新任务没有发给该节点，恢复后重新接单。
2. 任务 `27` 在运行中取消后状态为 `CANCELLED`，fencing 代次立即推进，旧 Worker 的进度、缓存、APK 和日志回写均被拒绝；任务 `28` 由手工重试创建并成功完成。
3. 任务 `32` 的原 Worker 暂停并使租约过期后，另一节点以 attempt 2 恢复并成功；旧 attempt 恢复后无法覆盖结果。
4. 修复过期 Worker 上传补偿后再次用任务 `33` 验证：旧 attempt 已上传但未登记的缓存对象和日志对象均被删除，数据库登记数与 MinIO 精确对象检查均为 0。

## 4. 缓存证据

1. 任务 `16` 真实构建并发布缓存，任务 `17` 使用相同不可变输入命中缓存。
2. 缓存命中采用两阶段确认：Worker 先下载并校验对象存在、大小和 SHA，再发送 `confirm_hit`；任务 `30` 确认命中后命中计数才从 2 增至 3。
3. 人为损坏实际 MinIO 缓存对象后，任务 `31` 产生 `CACHE_DOWNLOAD_VALIDATION_FAILED`，未增加命中计数，自动执行真实构建并恢复缓存。
4. 缓存键单元测试确认输入摘要、工具链版本或构建协议任一变化都会生成不同 SHA-256 键。
5. TTL 清理把未引用缓存对象推进到对象回收；仍被成功任务引用的对象只失效缓存条目，不删除物理对象。
6. LRU 验收选择未命中且无任务引用的条目回收，保留有成功任务引用的有效条目。

## 5. 隔离、一致性和可观测性

公平调度验收中，租户 2 使用与租户 1 相同的输入摘要解析缓存，结果为 `hit=false`。压力验收后执行全量不变量查询，以下异常数全部为 0：

- 缓存对象、源 APK、构建 APK、构建日志和签名配置的租户错配。
- APK 与日志对象 Key 的租户前缀错配。
- 同一任务代次重复槽位、非运行任务占用槽位、运行任务缺少槽位。
- 成功任务缺失 APK，或任务 APK 的 SHA/大小与对象元数据不一致。

`t_build_scheduler_event` 已记录公平领取、完成、Drain、取消、重试、缓存命中/失效和租约恢复原因。只读脚本复核到：22 条公平领取、3 条确认缓存命中、2 条缓存损坏、2 条租约恢复、1 条用户取消和 1 条手工重试事件。

新增 `GET /admin/core/build-cluster/metrics` 后，24 小时窗口运行结果包括：3 个在线节点、6 个总槽位、当前无排队/运行任务、成功率 100%，并能返回平均排队/构建时长、缓存命中率、缓存占用、租约恢复和缓存校验失败告警摘要。该接口及页面已完成 RBAC 权限迁移和 Docker 运行验证。

## 6. 自动化与部署结果

- `appforge-api`、`common`、4 个 Proto 模块、`services/system`、`services/core`、`services/builder` 的 `go test ./...` 全部通过。
- 前端 `npm run type-check` 和 `npm run build` 通过。
- Core Proto、Core RPC 和 API 生成代码已按项目 Makefile 重新生成。
- `core-rpc`、`admin-api`、`admin-ui` 已重新构建并部署；三个 Builder Worker 与其他长期服务运行正常。
- `./deploy/acceptance/v4-build-cluster.sh` 全项通过。

上述 2026-08-14 历史结果与 2026-08-15 当前恢复后复验均通过；按“当前失败证据优先”的状态规则，V4 已重新标记为当前完成。
