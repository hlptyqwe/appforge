# AppForge V2 基础白标实施规格

## 1. 目标

V2 在 V1 的渠道 APK 重打包能力上增加应用级基础白标，使租户能够为同一应用版本配置：

- 应用显示名称 `AppName`
- Launcher Logo
- 启动图
- 业务 `API Host`

V2 不修改 Android packageName，不引入源码托管，也不允许通过任意脚本修改 APK。

## 2. 当前基线与缺口

当前已有：

- `t_app_application.app_name`、`icon_url`、`api_host`。
- Builder 将 `apiHost` 写入 `assets/appforge/channel.json`。
- APK、Keystore、构建产物和日志的私有对象链路。

当前缺少：

- Logo、启动图的受控上传对象类型和元数据。
- 可版本化的品牌配置业务对象。
- 对 AndroidManifest、resources.arsc 和 drawable 资源的真实替换。
- 源 APK 是否支持白标的预检与错误报告。
- 白标预览、构建快照和运行验收。

仅保存 URL 或把值写入 JSON 不算 AppName、Logo、启动图替换完成。

## 3. 范围

### 3.1 包含

- 应用级品牌配置的创建、编辑、启用、停用和查询。
- PNG/WebP Logo 与启动图的浏览器直传、校验和私有存储。
- 白标 APK 兼容性预检。
- Builder 使用固定工具链解包、修改、重建、对齐、签名和验证 APK。
- 构建任务保存品牌配置快照，历史任务不受后续修改影响。
- 产物验证 AppName、Logo、启动图和 API Host。
- 管理端和代理端品牌配置页面。

### 3.2 不包含

- 修改 packageName。
- 每个渠道独立品牌配置。
- 多套白标模板和模板市场。
- Firebase、OAuth、推送证书替换。
- Android 源码 Gradle 构建。

以上能力进入 V3。

## 4. 白标兼容协议

V2 支持两种受控模式：

1. `RESOURCE_REBUILD`：通过固定版本 `apktool` 解码并重建 APK，适合标准 Android 资源。
2. `RUNTIME_CONTRACT`：客户 APK 已实现 AppForge Runtime Contract，从 `assets/appforge/branding.json` 读取运行时 API Host 和启动图；Launcher 名称和图标仍必须通过资源重建验证。

源 APK 预检必须确认：

- 存在有效 `AndroidManifest.xml`。
- 可读取 packageName、minSdk、targetSdk 和版本信息。
- 应用 label 可解析为字符串或资源引用。
- Launcher icon 可解析为 mipmap/drawable 资源。
- 配置的启动图目标资源存在，或 APK 实现 Runtime Contract。
- `apktool decode` 和未修改重建可成功。

不兼容时必须在创建构建任务前返回明确原因，不能进入无法恢复的构建循环。

## 5. 数据模型

### 5.1 存储对象类型扩展

在 `StorageObjectType` 中增加：

- `BRAND_LOGO = 5`：品牌 Logo。
- `BRAND_SPLASH = 6`：品牌启动图。

对象继续使用 `t_storage_object`，并遵循上传中、已就绪、已绑定、已删除、失败的生命周期。

### 5.2 品牌配置表

新增 `t_app_branding_profile`：

```text
id
tenant_id
app_id
profile_name
app_name
logo_object_id
splash_object_id
api_host
rewrite_mode
launcher_icon_target
splash_resource_target
runtime_config_json
status
revision
create_by
create_time
update_time
```

约束：

- `(tenant_id, app_id, profile_name)` 唯一。
- Logo、启动图对象必须属于同一租户和应用，且状态为 READY/BOUND。
- `api_host` 只允许 HTTPS；本地开发可以显式允许 HTTP localhost。
- 修改配置时 `revision` 单调递增。

### 5.3 品牌兼容性预检表

新增 `t_branding_preflight`：

```text
id
tenant_id
app_id
branding_profile_id
branding_revision
version_id
status
report_json
source_apk_sha256
toolchain_version
start_time
finish_time
create_by
create_time
update_time
```

同一租户、品牌配置、品牌修订和应用版本只保留一条预检记录。报告必须结构化保存兼容性结论、检查项和可定位的失败原因。

### 5.4 构建任务快照

`t_build_task` 增加：

```text
branding_profile_id
branding_revision
branding_snapshot JSON
```

任务创建时固化全部品牌字段和对象 ID。Builder 只读取任务快照，不读取会变化的当前品牌配置。

## 6. API 与 RPC

### 6.1 品牌配置 API

代理端和管理端提供：

```text
POST   /core/branding-profiles
PUT    /core/branding-profiles/:id
GET    /core/branding-profiles/:id
GET    /core/branding-profiles
POST   /core/branding-profiles/:id/status
POST   /core/branding-profiles/:id/preflight
```

构建任务创建请求增加可选 `brandingProfileId`。为空时按 V1 方式仅注入渠道 JSON。

### 6.2 Core RPC

增加：

- CreateBrandingProfile
- UpdateBrandingProfile
- GetBrandingProfile
- ListBrandingProfiles
- ChangeBrandingProfileStatus
- CreateBrandingPreflight
- CompleteBrandingPreflight

`GetBuildExecutionContext` 返回不可变品牌快照和 Logo/启动图对象元数据。

### 6.3 Builder RPC

增加品牌预检任务的领取和回报，或在现有构建任务类型中通过明确 `task_type` 区分。不得通过普通构建失败来模拟预检结果。

## 7. Builder 实现

V2 Builder 固定步骤：

```text
校验源 APK/Object SHA
↓
校验品牌资源 MIME、尺寸和 SHA
↓
apktool decode
↓
替换应用名称资源
↓
生成各 density Launcher Logo
↓
替换启动图目标资源或 Runtime Contract 资源
↓
写入 assets/appforge/branding.json
↓
写入 assets/appforge/channel.json
↓
apktool build
↓
zipalign
↓
apksigner sign + verify
↓
aapt 验证包名未变化、label 正确
↓
解包验证资源和 JSON SHA
↓
上传产物和构建日志
```

工具命令必须使用参数数组，路径和资源名经过白名单校验。临时目录权限为 `0700`，任务结束后删除。

## 8. 图片约束

Logo：

- PNG 或无动画 WebP。
- 正方形。
- 512×512 至 2048×2048。
- 最大 5 MiB。

启动图：

- PNG 或无动画 WebP。
- 最小短边 720 像素。
- 最大 10 MiB。

服务端必须读取真实图片头、尺寸和格式，不能只信扩展名或 Content-Type。

## 9. 前端

新增“品牌配置”页面：

- 应用选择器。
- 配置名称、AppName、API Host。
- Logo 和启动图上传及预览。
- APK 资源目标配置。
- 兼容性预检按钮和报告。
- 启用/停用与修订版本显示。

构建任务页面增加品牌配置选择器，并显示快照修订号。

所有业务选择使用下拉选择器，不要求客户手工输入 appId、versionId、objectId。

## 10. 安全与审计

- 图片对象必须私有，预览使用短时签名 URL。
- SVG 不在 V2 上传范围，避免脚本和外部实体风险。
- `api_host`、资源目标和配置 JSON 进行严格校验。
- 品牌配置创建、修改、状态变更和构建均写操作审计。
- 构建日志不得包含对象存储凭据或签名密码。

## 11. 实施顺序

### 阶段 A：模型与上传

- 扩展对象枚举和迁移。
- 增加品牌配置表、模型、Proto 和 API。
- 增加图片真实格式与尺寸校验。

### 阶段 B：预检

- Builder 镜像加入固定版本 apktool 和图片处理工具。
- 实现源 APK 兼容性预检和报告。
- 前端展示报告并阻止不兼容配置进入构建。

### 阶段 C：构建

- 实现资源解码、替换、重建、签名和产物验证。
- 构建任务保存并使用品牌快照。
- 增加失败清理和租约恢复测试。

### 阶段 D：前端与验收

- 增加品牌配置页面和构建选择器。
- Docker 全量重建。
- 使用真实模板 APK 完成端到端验收。

## 12. 验收标准

必须同时满足：

1. 租户只能查看和使用自己的品牌配置与图片对象。
2. 非图片、伪造 MIME、尺寸越界和动画图片上传均被拒绝。
3. 不兼容 APK 的预检返回可定位原因，不创建正式构建任务。
4. 创建品牌构建任务后自动进入 SUCCESS。
5. `aapt` 读取的 packageName 与源 APK 完全一致。
6. `aapt` 或资源解包读取的应用名称等于配置值。
7. Launcher Logo 和启动图产物 SHA 与品牌快照对应。
8. `branding.json` 中 API Host 和品牌修订号正确。
9. 修改品牌配置后，历史任务和历史产物保持原快照不变。
10. 过期 Worker 不能覆盖恢复后的品牌构建结果。
11. `apksigner verify` 通过，APK 可安装并启动。
12. Go 测试、前端类型检查/生产构建、Docker 全量重建通过。

只有以上验收都有实际运行证据，V2 才能标记完成。
