# AppForge V5 开放平台验收报告

## 1. 当前结论

验收日期：2026-08-14。

V5 的 Open API、API Key、Webhook、CLI、CI/CD 示例、GitHub/GitLab 受控集成、源码事件触发、开发者中心、审计与 Docker 独立 Worker 均已落地。规格中的 11 项验收标准已有单元测试、运行时调用、数据库不变量和全量部署证据，当前状态为 `完成`。

开放平台没有重复新建 Web 或 API 工程：

- 开发者中心位于现有 `appforge-ui`，管理端使用 5173，客户/代理端使用 5174 和独立 Agent 路由。
- 管理接口与 `/open/v1` 位于现有 `appforge-api`，通过 JWT/RBAC 与 API Key/Scope 两套边界隔离。
- `appforgectl` 是独立可分发 CLI；Webhook 和源码触发使用独立 Worker 进程。

仓库提供只读复核脚本 [v5-open-platform.sh](./deploy/acceptance/v5-open-platform.sh)，用于检查容器、迁移、密钥摘要、幂等、审计、Outbox、Webhook SSRF、源码事件幂等和敏感日志不变量。

2026-08-15 再次执行该脚本，当前容器/UI 路由及全部数据库不变量通过；0600 机器证据见 `docs/enterprise/evidence/v5-open-platform-readonly-20260815.json`。该证据不创建或轮换 API Key、不重放 CLI 上传构建下载流程，也不访问真实 GitHub/GitLab 凭据；这些能力仍由 2026-08-14 的运行记录与当前代码测试共同支撑。

## 2. 验收标准结果

| 序号 | 验收标准 | 结果与证据 |
| --- | --- | --- |
| 1 | Secret 只显示一次且不明文落库/日志 | API Key 数据库仅保存 64 位 SHA-256；操作审计递归脱敏；历史验收日志已清理；运行检查明文特征为 0 |
| 2 | Scope、应用、租户、IP、过期生效 | 逐项运行请求均按预期允许或返回 403，租户与应用范围不能由请求扩大 |
| 3 | 幂等键不重复创建 | 相同键和请求重放得到同一构建任务 43；修改请求体后返回 409；数据库无重复组合键 |
| 4 | Open API 全流程 | CLI 完成 APK 上传、版本创建、构建等待和产物下载；版本 11、构建 42、产物对象 114，下载文件 17,507 字节 |
| 5 | Webhook 可靠投递 | 构建 queued/started/succeeded/failed 事件写入 Outbox；投递记录唯一；失败达到上限进入死信 |
| 6 | 签名、重放与 SSRF 防护 | HMAC 验签与重复投递幂等通过；私网目标被 API 和 Worker 双重阻止，未发出网络请求 |
| 7 | CLI 非交互 CI | 仅使用环境变量即可运行；上传设置 Content-Length；标准流水线成功且没有输出凭证 |
| 8 | GitHub/GitLab 授权边界 | 两个平台签名事件均通过运行验证；未授权仓库、错误签名/令牌和不匹配 ref/job 被拒绝 |
| 9 | API Key 吊销与轮换 | 轮换过渡期新旧 Key 均有效；吊销旧 Key 后立即失效，新 Key 不受影响 |
| 10 | 限流、审计、错误码、文档一致 | 速率头和 429 已运行验证；Open API 审计包含 27 条、10 个路径及 200/403/409；开发者写操作进入操作日志 |
| 11 | 全量测试与 Docker 重建 | 10 个 Go 模块、前端类型/生产构建、V5 验收脚本和 Docker 全量重建全部通过 |

## 3. Open API、CLI 与 CI 证据

1. API Key 支持 Scope、应用白名单、IP/CIDR、有效期、每分钟限流、轮换过渡和立即吊销。Key ID 与 Secret 的组合解析支持 Base64URL 中的下划线，不再错误拆分。
2. Open API 提供统一请求 ID、结构化错误码、游标分页、限流响应头和写请求幂等。当前数据库有 8 条已完成幂等记录，没有重复键。
3. `appforgectl` 支持配置文件与纯环境变量两种非交互模式，覆盖 app list、version upload、build create/wait 和 artifact download。MinIO 预签名 PUT 会携带准确 Content-Length。
4. 实际 CLI 流程上传测试 APK，创建版本 11，构建 42 成功并下载对象 114；相同幂等键创建构建 43 时返回重放标识，改变载荷返回 409。
5. GitHub Action、GitLab CI Template 和通用 shell/CLI 示例已纳入仓库，示例不打印 API Key 或短时下载签名参数。

## 4. Webhook 与代码平台证据

### 4.1 出站 Webhook

- 业务事件与构建状态变更通过 Outbox 可靠衔接，Worker 独立领取、租约、重试和死信。
- 接口层拒绝私网端点；Worker 对数据库中的私网测试端点再次执行 DNS/IP 安全校验，投递状态进入死信、attempt 为 1，且没有发送请求。
- eventId 与 endpointId 唯一约束防止重复生成投递记录，签名协议使用 timestamp 加原始 body 的 HMAC-SHA256。

### 4.2 GitHub/GitLab 入站触发

- GitHub Release 事件通过 HMAC 校验、仓库/Tag/附件策略匹配和供应商投递号幂等；重复事件只保留一条，错误签名和其他仓库事件不入库。
- GitLab Pipeline 事件通过 Secret Token 校验，只选择指定分支、成功状态和精确 Job 名；重复事件幂等，错误令牌被拒绝。
- 两个平台事件均由独立 `source-trigger-worker` 领取。验收使用不可解密的隔离凭据，因此 Worker 在访问公网供应商前安全失败，没有向外部服务发送请求；真实 OAuth 成功链路需要部署方提供自己的 GitHub/GitLab 应用凭据。
- 验收集成、仓库、触发器和密文夹具均已停用或覆盖为撤销值，不会继续触发后台任务。

## 5. 安全与审计

- API Key 只保存不可逆摘要；OAuth Token、Webhook Secret 和源码触发签名 Secret 使用 secretbox 密文。
- 公开源码回调具备限流，RBAC 只放行指定 `/public/v1` 路径；请求日志不保存查询串，并把回调路径令牌替换为 `[REDACTED]`。
- 操作审计对对象、数组和 JSON 字符串递归脱敏；API Key、Secret、Token、密码、密文和签名 URL 不入库。非 JSON 或损坏 JSON 只记录安全占位。
- 本次发现的 11 条历史请求载荷和 1 条历史响应载荷已精确替换为安全占位；清理后敏感字段命中数为 0。新建临时 Key 的审计记录保存 `***`，API Key 明文特征为 0，随后临时凭据已吊销。

## 6. 本阶段修复的关键问题

1. MySQL 8.4 不支持迁移中的 `ADD COLUMN IF NOT EXISTS`，已改为 information_schema 检测和动态 DDL。
2. API Key 的 Base64URL Key ID 含下划线时解析失败，已按固定 Secret 长度从右侧解析并保留兼容回退。
3. CLI 在纯 CI 环境仍先读取本地配置，已改为支持环境变量独立启动。
4. MinIO 预签名 PUT 缺失 Content-Length 返回 411，已修复并增加传输层测试。
5. 公开源码回调曾被 RBAC 拦截、请求体被提前消费、负缓存导致重复事件返回数据库错误，均已修复并增加回归测试。
6. UI 容器曾缓存重建前的 API 容器 IP，导致 5174 持续 502 和空白页；现已使用 Docker DNS 动态解析，UI 健康检查改为静态根页面。API 单独重建不再使前端失联。

## 7. 自动化与部署结果

- `common`、4 个 Proto 模块、`services/system`、`services/core`、`services/builder`、`appforge-api` 和 `appforgectl` 的 `go test ./...` 全部通过。
- 前端 `npm run type-check` 与 `npm run build` 通过；仅有 Element Plus 大分包提示，不影响产物。
- Docker Compose 已全量重新构建和部署。Admin API、System/Core/Builder RPC、5173/5174 UI 均 healthy；三个 Builder Worker、Webhook Worker 和源码触发 Worker持续运行。
- 重建后的 [v5-open-platform.sh](./deploy/acceptance/v5-open-platform.sh) 全项通过；最终日志扫描没有 panic、fatal、错误级别日志、连接拒绝或 502。

至此，V5 可以按路线图标记完成，并进入 V6 商业化实施。
