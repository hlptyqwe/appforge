# AppForge V6 商业化实施规格

## 1. 目标

V6 在稳定 Open API 和构建集群之上增加 SaaS 商业化能力：

- 套餐与订阅。
- 构建并发、构建次数和存储额度。
- 用量计量和账单。
- 在线支付与人工合同订阅。
- 团队席位和权限限制。

## 2. 原则

- 权益与支付解耦：支付失败不会直接破坏业务数据。
- 用量以不可变账本记录，不依赖可被覆盖的计数器作为唯一依据。
- 限额检查在创建资源和调度执行两个阶段都进行。
- Webhook、支付回调和用量写入全部幂等。
- 金额使用最小货币单位整数，不使用浮点数。

## 3. 数据模型

### 3.1 套餐 `t_billing_plan`

```text
id, plan_code, plan_name, billing_cycle
price_amount, currency, feature_json
builds_per_cycle, max_build_concurrency
storage_bytes, team_seats, api_rate_limit
status, version, create_time, update_time
```

已被订阅引用的套餐版本不可原地修改。

### 3.2 订阅 `t_tenant_subscription`

```text
id, tenant_id, plan_id, plan_version
status, source, external_customer_id, external_subscription_id
current_period_start, current_period_end
cancel_at_period_end, grace_until
create_time, update_time
```

### 3.3 权益快照 `t_tenant_entitlement`

保存当前有效的构建、并发、存储、席位和 API 额度，来源可为套餐、合同或平台赠送。

### 3.4 用量账本 `t_usage_ledger`

```text
id, tenant_id, metric, quantity
resource_type, resource_id, idempotency_key
occurred_at, period_key, metadata, create_time
```

`(tenant_id, metric, idempotency_key)` 唯一。

### 3.5 账单与支付

- `t_invoice`
- `t_invoice_item`
- `t_payment_transaction`
- `t_billing_webhook_event`

支付提供商原始 payload 加密或脱敏保存，并设置保留期限。

## 4. 计量指标

至少包括：

- `build.started`
- `build.succeeded`
- `build.compute_seconds`
- `storage.source_bytes`
- `storage.artifact_bytes`
- `storage.log_bytes`
- `api.requests`
- `team.active_seats`

对象删除、构建失败、缓存命中和重试的计量规则必须在套餐中明确。

## 5. 限额执行

### 5.1 创建时

- 上传前检查存储额度和单文件上限。
- 创建构建前检查周期构建次数和订阅状态。
- 创建成员前检查席位。
- Open API 检查套餐速率限制。

### 5.2 调度时

- Builder 调度使用权益快照限制租户并发。
- 权益过期或订阅进入暂停状态时，不领取新任务。
- 已开始任务默认允许完成，除非平台安全策略要求取消。

### 5.3 宽限与告警

- 支持 70%、90%、100% 阈值通知。
- 支持有限宽限期和平台人工临时额度。
- 超额响应返回稳定业务错误码和当前用量/限额。

## 6. 支付

支付适配接口支持至少一个实际提供商和人工合同模式：

- Checkout/支付链接。
- 支付成功、失败、退款和争议回调。
- 订阅续费、升级、降级和周期末取消。
- 回调签名验证、事件幂等和乱序处理。

生产 Secret 必须由 Secret Manager 注入。

## 7. API/RPC

管理端：套餐、价格、合同和全租户用量。

代理端：

```text
GET  /billing/plans
GET  /billing/subscription
POST /billing/checkout
POST /billing/subscription/change
POST /billing/subscription/cancel
GET  /billing/usage
GET  /billing/invoices
```

内部 RPC 提供权益解析、额度预占、确认、释放和用量入账。额度预占与构建任务创建必须保持事务一致或使用可靠 outbox 补偿。

## 8. 前端

- 套餐对比和订阅管理。
- 构建、存储、API 和席位用量仪表盘。
- 阈值告警和超额说明。
- 账单、支付状态和发票列表。
- 平台运营的套餐版本、人工合同和临时额度管理。

## 9. 安全与财务一致性

- 金额、币种、税费和退款使用不可变账单项。
- 支付回调先验签再解析业务字段。
- 客户不能通过前端价格或 planId 覆盖服务端价格。
- 财务状态变化写独立审计。
- 用量重算工具只能追加调整账，不直接改历史账本。

## 10. 实施顺序

1. 套餐版本、订阅、权益和用量账本。
2. 构建/存储/席位/API 限额执行。
3. 用量页面、阈值和通知。
4. 支付适配、账单和回调。
5. 对账、重算、退款和故障验收。

## 11. 验收标准

1. 套餐版本变更不影响历史订阅和账单。
2. 构建次数、并发、存储、席位和 API 限额均在并发请求下不超卖。
3. 用量事件重复投递不会重复计费，遗漏事件可重放恢复。
4. 达到阈值产生一次幂等通知，超额请求返回稳定错误。
5. 升级、降级、取消、宽限和恢复状态正确。
6. 支付成功、失败、退款、重复和乱序回调处理正确。
7. 账单金额使用整数且可从不可变账本重算一致。
8. 缓存命中、失败构建和重试按套餐规则准确计量。
9. 租户只能查看自己的订阅、用量和账单。
10. 支付和额度故障不会破坏已有 APK、任务和归因数据。
11. 全量测试、并发额度测试、支付沙箱测试和 Docker 重建通过。

只有全部验收有运行证据，V6 才能标记完成。
